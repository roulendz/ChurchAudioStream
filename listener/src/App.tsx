import "./App.css";

/**
 * Root application shell for the Church Audio Stream listener PWA.
 * Plan 02 replaces this placeholder with the real channel list + player views.
 */
function App() {
  return (
    <div className="app-container">
      <h1 className="app-title">Church Audio Stream</h1>
      <p className="app-status">Loading channels...</p>
    </div>
  );
}

export default App;
