---
phase: 12-sidebar-header
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/badge.tsx
  - src/components/ui/breadcrumb.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/tooltip.tsx
  - src/components/layout/Sidebar.tsx
  - src/__tests__/sidebar.test.tsx
autonomous: true
requirements: [SIDE-01, SIDE-02, SIDE-03, TYPO-02]

must_haves:
  truths:
    - "Each sidebar nav item shows a Lucide icon next to its label"
    - "Active nav item has colored vertical indicator bar + background highlight"
    - "Nav items grouped into Main and System with visual separator between"
    - "Each group has uppercase label heading above its items"
  artifacts:
    - path: "src/components/ui/badge.tsx"
      provides: "shadcn Badge primitive"
    - path: "src/components/ui/breadcrumb.tsx"
      provides: "shadcn Breadcrumb primitive"
    - path: "src/components/ui/separator.tsx"
      provides: "shadcn Separator primitive"
    - path: "src/components/ui/tooltip.tsx"
      provides: "shadcn Tooltip primitive"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Nav with icons, groups, separators, active bar"
      contains: "NAV_GROUPS"
    - path: "src/__tests__/sidebar.test.tsx"
      provides: "SIDE-01, SIDE-02, SIDE-03, TYPO-02 tests"
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "lucide-react"
      via: "icon imports"
      pattern: "LayoutDashboard|Radio|Activity|Settings"
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/components/ui/separator.tsx"
      via: "Separator between nav groups"
      pattern: "Separator"
---

<objective>
Install shadcn UI primitives (Badge, Breadcrumb, Separator, Tooltip) and upgrade Sidebar with Lucide icons, nav groups, visual separators, active indicator bar, and section headings.

Purpose: Sidebar gets professional polish -- icons for recognition, grouping for organization, active bar for orientation. shadcn primitives installed for Plan 02 header work.
Output: 4 shadcn UI components installed, Sidebar.tsx upgraded, sidebar tests passing.
</objective>

<execution_context>
@C:/Users/lives/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lives/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sidebar-header/12-RESEARCH.md
@.planning/phases/12-sidebar-header/12-PATTERNS.md

<interfaces>
<!-- From src/components/layout/Sidebar.tsx (current) -->
```typescript
export type DashboardSection = "overview" | "channels" | "monitoring" | "settings";

interface SidebarProps {
  currentSection: DashboardSection;
  onNavigate: (section: DashboardSection) => void;
}
```

<!-- From src/lib/utils.ts -->
```typescript
import { cn } from "@/lib/utils";
```

<!-- From src/components/ui/button.tsx (shadcn pattern) -->
```typescript
import * as React from "react"
import { cva, type VariantProps } from "class-variance-authority"
import { Slot } from "radix-ui"
import { cn } from "@/lib/utils"
```

<!-- Test pattern from src/__tests__/design-tokens.test.tsx -->
```typescript
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn UI primitives</name>
  <files>src/components/ui/badge.tsx, src/components/ui/breadcrumb.tsx, src/components/ui/separator.tsx, src/components/ui/tooltip.tsx</files>
  <read_first>components.json, src/components/ui/button.tsx</read_first>
  <action>
Run the shadcn CLI to install all 4 components at once:

```bash
npx shadcn@latest add badge breadcrumb separator tooltip
```

This uses the existing `components.json` configuration (style: new-york, rsc: false, tsx: true, aliases configured). The CLI generates files into `src/components/ui/`.

After installation, verify each file exists and exports correctly. No manual edits needed -- these are CLI-generated.

If the CLI prompts for overwrite confirmation (unlikely since these are new), accept defaults.
  </action>
  <verify>
    <automated>ls src/components/ui/badge.tsx src/components/ui/breadcrumb.tsx src/components/ui/separator.tsx src/components/ui/tooltip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/badge.tsx` exists and contains `export { Badge, badgeVariants }`
    - `src/components/ui/breadcrumb.tsx` exists and contains `export { Breadcrumb`
    - `src/components/ui/separator.tsx` exists and contains `export { Separator }`
    - `src/components/ui/tooltip.tsx` exists and contains `export { TooltipProvider`
  </acceptance_criteria>
  <done>All 4 shadcn primitives installed in src/components/ui/, each file exports its named component.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Sidebar with icons, groups, separators, active bar, headings</name>
  <files>src/components/layout/Sidebar.tsx</files>
  <read_first>src/components/layout/Sidebar.tsx, src/components/ui/separator.tsx, .planning/phases/12-sidebar-header/12-RESEARCH.md</read_first>
  <action>
Rewrite `src/components/layout/Sidebar.tsx` with these changes. Keep `DashboardSection` type export and `SidebarProps` interface unchanged.

**1. Add imports** (per SIDE-01, SIDE-03):
```typescript
import { LayoutDashboard, Radio, Activity, Settings, type LucideIcon } from "lucide-react";
import { Separator } from "@/components/ui/separator";
import { cn } from "@/lib/utils";
```

**2. Replace NavItem interface and NAV_ITEMS** with grouped structure (per SIDE-03):
```typescript
interface NavItem {
  section: DashboardSection;
  label: string;
  icon: LucideIcon;
}

interface NavGroup {
  label: string;
  items: NavItem[];
}

const NAV_GROUPS: NavGroup[] = [
  {
    label: "Main",
    items: [
      { section: "overview", label: "Overview", icon: LayoutDashboard },
      { section: "channels", label: "Channels", icon: Radio },
    ],
  },
  {
    label: "System",
    items: [
      { section: "monitoring", label: "Monitoring", icon: Activity },
      { section: "settings", label: "Settings", icon: Settings },
    ],
  },
];
```

**3. Update Sidebar render** to iterate groups with separators (per SIDE-03), render icons (per SIDE-01), show group label headings (per TYPO-02), and enhanced active indicator (per SIDE-02):

```typescript
export function Sidebar({ currentSection, onNavigate }: SidebarProps) {
  return (
    <nav
      className="row-start-2 bg-card border-r border-border py-4 overflow-y-auto"
      aria-label="Dashboard navigation"
    >
      {NAV_GROUPS.map((group, groupIndex) => (
        <div key={group.label}>
          {groupIndex > 0 && <Separator className="my-2 mx-3" />}
          <p className="px-5 py-1.5 text-xs font-medium text-muted-foreground uppercase tracking-wider">
            {group.label}
          </p>
          {group.items.map(({ section, label, icon: Icon }) => {
            const isActive = section === currentSection;
            return (
              <button
                key={section}
                type="button"
                className={cn(
                  "flex items-center gap-3 w-full px-5 py-2.5",
                  "border-l-[3px] border-l-transparent",
                  "text-muted-foreground text-sm text-left cursor-pointer",
                  "transition-all duration-150",
                  "hover:bg-accent/50 hover:text-foreground",
                  isActive && "border-l-primary text-primary bg-primary/10 font-medium"
                )}
                onClick={() => onNavigate(section)}
                aria-current={isActive ? "page" : undefined}
              >
                <Icon className="size-4 shrink-0" />
                <span>{label}</span>
              </button>
            );
          })}
        </div>
      ))}
    </nav>
  );
}
```

Key differences from current code:
- `icon: Icon` destructured from each item, rendered as `<Icon className="size-4 shrink-0" />` (SIDE-01)
- `border-l-primary text-primary bg-primary/10 font-medium` on active item -- adds bg highlight + font weight (SIDE-02)
- `NAV_GROUPS` with `Separator` between groups (SIDE-03)
- Group `<p>` label headings: `text-xs font-medium uppercase tracking-wider` (TYPO-02)
- Removed `bg-transparent border-0 font-[inherit]` and `hover:bg-white/[0.04]` (replaced with cleaner hover: `hover:bg-accent/50`)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File imports `LayoutDashboard, Radio, Activity, Settings` from `lucide-react`
    - File imports `Separator` from `@/components/ui/separator`
    - `NAV_GROUPS` array exists with two groups ("Main", "System")
    - Each nav button renders `<Icon className="size-4 shrink-0" />`
    - Active state includes `bg-primary/10 font-medium` (not just border color)
    - Group headings render with `uppercase tracking-wider`
    - `<Separator` rendered between groups
    - `DashboardSection` type still exported
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Sidebar renders Lucide icons on each nav item, groups separated by Separator, active indicator has colored bar + bg highlight + font-medium, group labels uppercase. Type export preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Create sidebar tests</name>
  <files>src/__tests__/sidebar.test.tsx</files>
  <read_first>src/components/layout/Sidebar.tsx, src/__tests__/design-tokens.test.tsx, src/components/CheckForUpdatesButton/CheckForUpdatesButton.test.tsx</read_first>
  <action>
Create `src/__tests__/sidebar.test.tsx` following project test patterns (vitest + testing-library, dynamic imports).

```typescript
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";

describe("Sidebar navigation", () => {
  async function renderSidebar(currentSection = "overview" as const) {
    const { Sidebar } = await import("@/components/layout/Sidebar");
    return render(
      <Sidebar currentSection={currentSection} onNavigate={() => {}} />
    );
  }

  it("renders Lucide icon SVG in each nav item (SIDE-01)", async () => {
    await renderSidebar();
    const nav = screen.getByRole("navigation", { name: /dashboard navigation/i });
    const buttons = nav.querySelectorAll("button");
    expect(buttons.length).toBe(4);
    for (const button of buttons) {
      const svg = button.querySelector("svg");
      expect(svg).toBeTruthy();
    }
  });

  it("active nav item has indicator bar and background highlight (SIDE-02)", async () => {
    await renderSidebar("channels");
    const activeButton = screen.getByRole("button", { name: /channels/i });
    expect(activeButton.className).toContain("border-l-primary");
    expect(activeButton.className).toContain("bg-primary");
    expect(activeButton.getAttribute("aria-current")).toBe("page");
  });

  it("inactive nav items have transparent border (SIDE-02)", async () => {
    await renderSidebar("overview");
    const settingsButton = screen.getByRole("button", { name: /settings/i });
    expect(settingsButton.className).toContain("border-l-transparent");
    expect(settingsButton.getAttribute("aria-current")).toBeNull();
  });

  it("renders separator between nav groups (SIDE-03)", async () => {
    await renderSidebar();
    const separators = screen.getAllByRole("separator");
    expect(separators.length).toBeGreaterThanOrEqual(1);
  });

  it("renders group label headings with uppercase styling (TYPO-02)", async () => {
    await renderSidebar();
    expect(screen.getByText("Main")).toBeInTheDocument();
    expect(screen.getByText("System")).toBeInTheDocument();
    const mainLabel = screen.getByText("Main");
    expect(mainLabel.className).toContain("uppercase");
    expect(mainLabel.className).toContain("tracking-wider");
  });

  it("renders all four nav items with correct labels", async () => {
    await renderSidebar();
    expect(screen.getByRole("button", { name: /overview/i })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: /channels/i })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: /monitoring/i })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: /settings/i })).toBeInTheDocument();
  });
});
```

Note: The `name` matcher on buttons matches text content including the `<span>` child. Lucide icons render as `<svg>` elements. Separator from shadcn/radix renders with `role="separator"`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/sidebar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/__tests__/sidebar.test.tsx` exists
    - Contains tests for SIDE-01 (icon SVGs), SIDE-02 (active indicator), SIDE-03 (separator), TYPO-02 (group headings)
    - All tests pass: `npx vitest run src/__tests__/sidebar.test.tsx` exits 0
  </acceptance_criteria>
  <done>6 sidebar tests pass covering all 4 requirements (SIDE-01, SIDE-02, SIDE-03, TYPO-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure client-side UI, no user input processing, no network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | N/A | Sidebar | accept | No security surface -- nav state is internal enum, no user-controlled input, no network calls. Pure cosmetic UI phase. |
</threat_model>

<verification>
```bash
# All sidebar tests pass
npx vitest run src/__tests__/sidebar.test.tsx

# TypeScript compiles
npx tsc --noEmit

# shadcn components exist
ls src/components/ui/badge.tsx src/components/ui/breadcrumb.tsx src/components/ui/separator.tsx src/components/ui/tooltip.tsx
```
</verification>

<success_criteria>
1. Four shadcn UI primitives installed in src/components/ui/
2. Sidebar shows Lucide icons on all 4 nav items
3. Active indicator has colored bar + bg highlight + font-medium
4. Nav items grouped into Main/System with Separator between
5. Group headings render uppercase with tracking
6. All sidebar tests pass
7. TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-sidebar-header/12-01-SUMMARY.md`
</output>
