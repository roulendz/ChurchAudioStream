# Phase 12: Sidebar & Header - Pattern Map

**Mapped:** 2026-05-05
**Files analyzed:** 9 (4 modified + 4 new shadcn + 1 new test)
**Analogs found:** 9 / 9

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `src/components/layout/Sidebar.tsx` | component | request-response | Self (current version) | exact |
| `src/components/layout/DashboardShell.tsx` | component | request-response | Self (current version) | exact |
| `src/components/ConnectionStatus.tsx` | component | request-response | Self (current version) | exact |
| `src/components/monitoring/ListenerCountBadge.tsx` | component | request-response | Self (current version) | exact |
| `src/components/ui/badge.tsx` | ui-primitive | N/A | `src/components/ui/button.tsx` | role-match |
| `src/components/ui/breadcrumb.tsx` | ui-primitive | N/A | `src/components/ui/button.tsx` | role-match |
| `src/components/ui/separator.tsx` | ui-primitive | N/A | `src/components/ui/button.tsx` | role-match |
| `src/components/ui/tooltip.tsx` | ui-primitive | N/A | `src/components/ui/button.tsx` | role-match |
| `src/__tests__/sidebar.test.tsx` | test | N/A | `src/__tests__/design-tokens.test.tsx` | role-match |
| `src/__tests__/header.test.tsx` | test | N/A | `src/components/CheckForUpdatesButton/CheckForUpdatesButton.test.tsx` | role-match |
| `src/App.tsx` | provider | request-response | Self (current version) | exact |

## Pattern Assignments

### `src/components/ui/badge.tsx` (ui-primitive, shadcn CLI generated)

**Analog:** `src/components/ui/button.tsx` -- installed via `npx shadcn@latest add badge`

**shadcn UI component conventions** (button.tsx lines 1-64):
```typescript
// Import pattern for shadcn ui components
import * as React from "react"
import { cva, type VariantProps } from "class-variance-authority"
import { Slot } from "radix-ui"
import { cn } from "@/lib/utils"

// CVA variant definition pattern
const badgeVariants = cva(
  "base-classes...",
  { variants: { variant: { default: "...", outline: "...", secondary: "...", destructive: "..." } } }
)

// Export both component + variants
export { Badge, badgeVariants }
```

**Note:** All 4 shadcn components (badge, breadcrumb, separator, tooltip) auto-generated by CLI. No manual authoring. Install command:
```bash
npx shadcn@latest add badge breadcrumb separator tooltip
```

---

### `src/components/layout/Sidebar.tsx` (component, request-response)

**Analog:** Self -- current file is the modification target.

**Current imports pattern** (lines 1-1):
```typescript
import { cn } from "@/lib/utils";
```

**Current NavItem type + data** (lines 4-21):
```typescript
export type DashboardSection = "overview" | "channels" | "monitoring" | "settings";

interface NavItem {
  section: DashboardSection;
  label: string;
}

const NAV_ITEMS: NavItem[] = [
  { section: "overview", label: "Overview" },
  { section: "channels", label: "Channels" },
  { section: "monitoring", label: "Monitoring" },
  { section: "settings", label: "Settings" },
];
```

**Current nav button pattern** (lines 23-48):
```typescript
export function Sidebar({ currentSection, onNavigate }: SidebarProps) {
  return (
    <nav className="row-start-2 bg-card border-r border-border py-4 overflow-y-auto" aria-label="Dashboard navigation">
      {NAV_ITEMS.map(({ section, label }) => {
        const isActive = section === currentSection;
        return (
          <button
            key={section}
            type="button"
            className={cn(
              "flex items-center gap-3 w-full px-5 py-2.5 border-l-[3px] border-l-transparent",
              "text-muted-foreground text-sm text-left cursor-pointer transition-all duration-150",
              "hover:bg-white/[0.04] hover:text-foreground",
              "bg-transparent border-0 font-[inherit]",
              isActive && "border-l-primary text-primary bg-primary/[0.08]"
            )}
            onClick={() => onNavigate(section)}
            aria-current={isActive ? "page" : undefined}
          >
            {label}
          </button>
        );
      })}
    </nav>
  );
}
```

**Modifications needed:**
1. Add `LucideIcon` type to NavItem interface + icon imports (SIDE-01)
2. Split NAV_ITEMS into NAV_GROUPS array with `label` + `items` (SIDE-03)
3. Add Separator between groups (SIDE-03)
4. Render icon alongside label in button (SIDE-01)
5. Enhance active indicator: add `font-medium` + `bg-primary/10` (SIDE-02)
6. Add group label headings with `text-xs font-medium uppercase tracking-wider` (TYPO-02)
7. Accept optional `visible` prop (or use conditional rendering from DashboardShell) for HEAD-04

---

### `src/components/layout/DashboardShell.tsx` (component, request-response)

**Analog:** Self -- current file is the modification target.

**Current imports** (lines 1-4):
```typescript
import type { ReactNode } from "react";
import type { ConnectionStatus as ConnectionStatusType } from "../../hooks/useWebSocket";
import { ConnectionStatus } from "../ConnectionStatus";
import { Sidebar, type DashboardSection } from "./Sidebar";
```

**Current props interface** (lines 6-12):
```typescript
interface DashboardShellProps {
  currentSection: DashboardSection;
  onNavigate: (section: DashboardSection) => void;
  connectionStatus: ConnectionStatusType;
  reconnectAttempts: number;
  children: ReactNode;
}
```

**Current header + grid layout** (lines 22-36):
```typescript
<div className="grid grid-cols-[220px_1fr] grid-rows-[auto_1fr] min-h-screen">
  <header className="col-span-full flex items-center justify-between px-5 py-3 border-b border-border bg-background sticky top-0 z-50">
    <h1 className="text-xl font-semibold text-foreground">Church Audio Stream - Admin</h1>
    <ConnectionStatus
      status={connectionStatus}
      reconnectAttempts={reconnectAttempts}
    />
  </header>
  <Sidebar currentSection={currentSection} onNavigate={onNavigate} />
  <main className="row-start-2 p-6 overflow-y-auto">{children}</main>
</div>
```

**Modifications needed:**
1. Add props: `totalListeners: number` (HEAD-03)
2. Add `sidebarVisible` state (useState, default true) (HEAD-04)
3. Header left side: replace h1 with Breadcrumb (HEAD-01) + SidebarToggle button (HEAD-04)
4. Header right side: ConnectionStatus Badge (HEAD-02) + ListenerCountBadge (HEAD-03)
5. Grid template: conditional `grid-cols-[220px_1fr]` vs `grid-cols-[1fr]` based on sidebarVisible (HEAD-04)
6. Sidebar: conditional render or `hidden` class based on sidebarVisible

---

### `src/components/ConnectionStatus.tsx` (component, request-response)

**Analog:** Self -- current file is the modification target.

**Current STATUS_DISPLAY map** (lines 9-23):
```typescript
const STATUS_DISPLAY: Record<
  ConnectionStatusType,
  { label: string; className: string }
> = {
  connected: { label: "Connected", className: "bg-success shadow-[0_0_6px] shadow-success" },
  connecting: { label: "Connecting...", className: "bg-warning animate-pulse" },
  reconnecting: { label: "Reconnecting", className: "bg-warning animate-pulse" },
  disconnected: { label: "Disconnected", className: "bg-destructive" },
};
```

**Current render pattern** (lines 36-41):
```typescript
<div className="flex items-center gap-2 text-sm px-3 py-1 rounded-full bg-card" role="status" aria-live="polite">
  <span className={cn("size-2 rounded-full shrink-0", className)} aria-hidden="true" />
  <span className="text-muted-foreground whitespace-nowrap">{displayLabel}</span>
</div>
```

**Modifications needed:**
1. Import `Badge` from `@/components/ui/badge`
2. Replace outer `<div>` with `<Badge variant="outline">` (HEAD-02)
3. Keep dot span + label span inside Badge
4. Keep `role="status"` + `aria-live="polite"` on Badge wrapper
5. Keep STATUS_DISPLAY map + reconnect logic unchanged

---

### `src/components/monitoring/ListenerCountBadge.tsx` (component, request-response)

**Analog:** Self -- current file is the modification target.

**Current inline SVG** (lines 21-29):
```typescript
<svg
  className="inline-flex items-center"
  width="14" height="14"
  viewBox="0 0 24 24"
  fill="currentColor"
  aria-hidden="true"
>
  <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z" />
</svg>
```

**Modifications needed:**
1. Import `Badge` from `@/components/ui/badge`
2. Import `Users` from `lucide-react` (replace inline SVG)
3. Replace outer `<span>` with `<Badge variant="secondary">`
4. Replace inline SVG with `<Users className="size-3.5" />`

---

### `src/App.tsx` (provider, request-response)

**Analog:** Self -- current file is the modification target.

**Current DashboardShell usage** (lines 60-65):
```typescript
<DashboardShell
  currentSection={currentSection}
  onNavigate={setCurrentSection}
  connectionStatus={connectionStatus}
  reconnectAttempts={reconnectAttempts}
>
```

**Modifications needed:**
1. Add `totalListeners` prop to DashboardShell (line ~60)
2. Wrap `<DashboardShell>` (or entire app) in `<TooltipProvider>` for shadcn Tooltip

**TooltipProvider placement** -- in App.tsx wrapping DashboardShell:
```typescript
import { TooltipProvider } from "@/components/ui/tooltip";

// In render:
<TooltipProvider>
  <DashboardShell totalListeners={totalListeners} ...>
    ...
  </DashboardShell>
</TooltipProvider>
```

---

### `src/__tests__/sidebar.test.tsx` (test, new file)

**Analog:** `src/__tests__/design-tokens.test.tsx` (lines 1-6, 74-95) + `src/components/CheckForUpdatesButton/CheckForUpdatesButton.test.tsx` (lines 1-8, 22-25)

**Test file import pattern** (design-tokens.test.tsx lines 1-2):
```typescript
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
```

**Component smoke test pattern** (design-tokens.test.tsx lines 74-88):
```typescript
it("DashboardShell renders without crash", async () => {
  const { DashboardShell } = await import("@/components/layout/DashboardShell");
  render(
    <DashboardShell
      currentSection="overview"
      onNavigate={() => {}}
      connectionStatus={"connected" as any}
      reconnectAttempts={0}
    >
      <div>test child</div>
    </DashboardShell>
  );
  expect(screen.getByText("test child")).toBeInTheDocument();
});
```

**Component render + assertion pattern** (CheckForUpdatesButton.test.tsx lines 22-26):
```typescript
it("renders title + Check now button", () => {
  render(<CheckForUpdatesButton />);
  expect(screen.getByText(/check for updates/i)).toBeInTheDocument();
  expect(screen.getByRole("button", { name: /check now/i })).toBeInTheDocument();
});
```

**Tests to write for sidebar:**
- SIDE-01: Each nav button contains an SVG icon (query `role="img"` or svg element)
- SIDE-02: Active nav item has `border-l-primary` class
- SIDE-03: `role="separator"` elements present between groups
- TYPO-02: Group label headings rendered

---

### `src/__tests__/header.test.tsx` (test, new file)

**Analog:** Same test patterns as sidebar.test.tsx above.

**Tests to write for header:**
- HEAD-01: Breadcrumb renders current section label
- HEAD-02: ConnectionStatus renders inside Badge with dot
- HEAD-03: Listener count visible in header
- HEAD-04: Toggle button toggles sidebar visibility (use userEvent.click pattern from CheckForUpdatesButton.test.tsx)

**userEvent click pattern** (CheckForUpdatesButton.test.tsx lines 39-48):
```typescript
import userEvent from "@testing-library/user-event";

it("clicking toggle hides sidebar", async () => {
  render(<DashboardShell ...>...</DashboardShell>);
  const user = userEvent.setup();
  const button = screen.getByRole("button", { name: /toggle sidebar/i });
  await user.click(button);
  // assert sidebar hidden
});
```

---

## Shared Patterns

### cn() Utility
**Source:** `src/lib/utils.ts` (lines 1-7)
**Apply to:** All component files (Sidebar, DashboardShell, ConnectionStatus, ListenerCountBadge)
```typescript
import { cn } from "@/lib/utils";
```

### shadcn Import Convention
**Source:** `src/components/ui/button.tsx` (lines 1-5)
**Apply to:** All new shadcn ui components (badge, breadcrumb, separator, tooltip)
```typescript
import * as React from "react"
import { cva, type VariantProps } from "class-variance-authority"
import { Slot } from "radix-ui"
import { cn } from "@/lib/utils"
```
Note: These are auto-generated by `npx shadcn@latest add`. No manual authoring.

### Path Alias Convention
**Source:** `components.json` (lines 14-19), `vitest.config.ts` (line 9)
**Apply to:** All imports across project
```typescript
// Always use @/ alias, never relative paths for cross-directory imports
import { cn } from "@/lib/utils";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
```

### Test File Convention
**Source:** `src/__tests__/design-tokens.test.tsx` (lines 1-2), `vitest.config.ts` (lines 17-20)
**Apply to:** All new test files
```typescript
// Tests in src/__tests__/ or colocated as *.test.tsx
// Use named imports from vitest (globals: false)
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
// Dynamic import for components (pattern from design-tokens.test.tsx)
const { ComponentName } = await import("@/components/path");
```

### DashboardSection Type
**Source:** `src/components/layout/Sidebar.tsx` (line 4)
**Apply to:** DashboardShell, App.tsx, breadcrumb, tests
```typescript
export type DashboardSection = "overview" | "channels" | "monitoring" | "settings";
```
This type stays in Sidebar.tsx and is re-exported. Used by DashboardShell and App.tsx.

### Lucide Icon Sizing Convention
**Source:** Research patterns, button.tsx SVG rule (line 8)
**Apply to:** All Lucide icon usage
```typescript
// button.tsx enforces: [&_svg:not([class*='size-'])]:size-4
// Explicit sizing for nav icons:
<Icon className="size-4 shrink-0" />
// For toggle button (slightly larger):
<PanelLeft className="size-5" />
```

## No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| (none) | -- | -- | All files have direct analogs (self-modification or shadcn CLI pattern) |

## Metadata

**Analog search scope:** `src/components/`, `src/__tests__/`, `src/hooks/`, `src/lib/`
**Files scanned:** 15
**Pattern extraction date:** 2026-05-05
