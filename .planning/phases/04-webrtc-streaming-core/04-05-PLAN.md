---
phase: 04-webrtc-streaming-core
plan: 05
type: execute
wave: 4
depends_on: ["04-01", "04-04"]
files_modified:
  - sidecar/src/streaming/streaming-subsystem.ts
  - sidecar/src/server.ts
  - sidecar/src/index.ts
  - sidecar/src/audio/audio-subsystem.ts
  - sidecar/src/ws/handler.ts
autonomous: true

must_haves:
  truths:
    - "StreamingSubsystem wires all streaming components and integrates with AudioSubsystem"
    - "When a channel starts/stops, the streaming layer creates/removes the Router and PlainTransport"
    - "When admin hides/stops a channel, listeners on it are disconnected and notified"
    - "Graceful shutdown order: notify listeners -> drain 5-10s -> close mediasoup -> close GStreamer"
    - "Listener WebSocket handler mounts on HTTPS server at /ws/listener"
    - "Admin WebSocket API exposes streaming stats (listener counts, worker status)"
  artifacts:
    - path: "sidecar/src/streaming/streaming-subsystem.ts"
      provides: "Streaming subsystem facade wiring all components"
      exports: ["StreamingSubsystem"]
  key_links:
    - from: "sidecar/src/streaming/streaming-subsystem.ts"
      to: "sidecar/src/audio/audio-subsystem.ts"
      via: "Listens to channel-state-changed events to sync streaming state"
      pattern: "audioSubsystem.on.*channel-state-changed"
    - from: "sidecar/src/index.ts"
      to: "sidecar/src/streaming/streaming-subsystem.ts"
      via: "Creates and starts StreamingSubsystem in main()"
      pattern: "streamingSubsystem"
    - from: "sidecar/src/server.ts"
      to: "sidecar/src/ws/listener-handler.ts"
      via: "Path-based upgrade routing for /ws/listener"
      pattern: "ws/listener"
---

<objective>
Create the StreamingSubsystem facade that wires all streaming components together, integrate it into the application lifecycle (server.ts, index.ts), and implement the graceful shutdown sequence.

Purpose: This connects the mediasoup streaming infrastructure to the existing audio pipeline, enabling end-to-end audio flow from GStreamer to browser listeners.
Output: StreamingSubsystem facade, updated server with listener WebSocket path, updated index.ts with streaming lifecycle.
</objective>

<execution_context>
@C:\Users\rolan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\rolan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-webrtc-streaming-core/04-CONTEXT.md
@.planning/phases/04-webrtc-streaming-core/04-04-SUMMARY.md
@.planning/phases/04-webrtc-streaming-core/04-01-SUMMARY.md
@sidecar/src/streaming/streaming-types.ts
@sidecar/src/streaming/worker-manager.ts
@sidecar/src/streaming/router-manager.ts
@sidecar/src/streaming/plain-transport-manager.ts
@sidecar/src/streaming/transport-manager.ts
@sidecar/src/streaming/signaling-handler.ts
@sidecar/src/ws/listener-handler.ts
@sidecar/src/audio/audio-subsystem.ts
@sidecar/src/server.ts
@sidecar/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StreamingSubsystem facade</name>
  <files>sidecar/src/streaming/streaming-subsystem.ts</files>
  <action>
    Create `sidecar/src/streaming/streaming-subsystem.ts` implementing StreamingSubsystem class.

    **Pattern:** Follow AudioSubsystem facade pattern -- single entry point for all streaming functionality, extends EventEmitter.

    **Constructor:** Takes ConfigStore, AudioSubsystem reference.

    **`start(httpsServer)` method:**
    1. Read config for mediasoup and streaming settings
    2. Create WorkerManager with mediasoup config, call `workerManager.start()`
    3. Create PlainTransportManager
    4. Create RouterManager with WorkerManager and PlainTransportManager references
    5. Create TransportManager with `config.server.host` as announced IP
    6. Create SignalingHandler with RouterManager, TransportManager, and channel metadata resolver
    7. Create ListenerWebSocketHandler on httpsServer with SignalingHandler and streaming config
    8. Wire AudioSubsystem events:
       - `audioSubsystem.on("channel-state-changed", (channelId, status))`:
         - When status is "streaming": Create channel router + PlainTransport if not already active
           - Get channel from AudioSubsystem for processing config (rtpPort, rtcpPort, ssrc)
           - Call `routerManager.createChannelRouter(channelId, rtpPort, rtcpPort, ssrc)`
           - Notify listeners: `signalingHandler.notifyAllListeners("channelStateChanged", { channelId, state: "active" })`
           - Push updated active channel list to all listeners
         - When status is "stopped" or "error" or "crashed":
           - Notify listeners on that channel: `signalingHandler.notifyListenersOnChannel(channelId, "consumerClosed", { reason: "channel-stopped" })`
           - Notify all listeners of updated channel list
           - Remove channel router: `routerManager.removeChannelRouter(channelId)`
       - `audioSubsystem.on("channel-removed", (channelId))`:
         - Same as stopped: notify listeners, remove router
       - `audioSubsystem.on("channel-created", (channel))`:
         - Notify listeners of new channel available (per locked decision: push "new channel available" notification)
    9. Wire WorkerManager events:
       - `workerManager.on("worker-died", ...)` -- log alert, emit for admin dashboard
       - `workerManager.on("worker-memory-warning", ...)` -- emit for admin dashboard
    10. Log streaming subsystem started

    **`stop()` method -- Graceful shutdown (per locked decision: specific order):**
    1. Notify all listeners: `signalingHandler.notifyAllListeners("serverShuttingDown", { reason: "Server shutting down" })`
    2. Wait drain period (config.streaming.shutdownDrainMs, default 5s) -- let clients receive the notification
    3. Close ListenerWebSocketHandler (disconnect all listeners)
    4. Close TransportManager (close all WebRtcTransports)
    5. Close RouterManager (close all routers, cascading to PlainTransports and producers)
    6. Close WorkerManager (close all workers)
    7. Log streaming subsystem stopped

    **Delegated accessors for admin dashboard:**
    - `getListenerCount(channelId?)` -- delegates to SignalingHandler
    - `getListenerSessions()` -- delegates to SignalingHandler
    - `getWorkerResourceInfo()` -- delegates to WorkerManager
    - `getActiveStreamingChannels()` -- delegates to RouterManager

    **Events emitted:**
    - `"listener-count-changed"` (channelId, count) -- for admin dashboard broadcast
    - `"worker-alert"` (type, details) -- for admin dashboard alerts
  </action>
  <verify>
    `npx tsc --noEmit -p sidecar/tsconfig.json` compiles with no errors.
    StreamingSubsystem can be imported.
  </verify>
  <done>StreamingSubsystem wires all streaming components, syncs with AudioSubsystem channel lifecycle, and implements graceful shutdown order.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate StreamingSubsystem into server.ts and index.ts lifecycle</name>
  <files>
    sidecar/src/server.ts
    sidecar/src/index.ts
  </files>
  <action>
    **Modify `sidecar/src/server.ts`:**
    1. Change WebSocketServer setup to use `noServer: true` for path-based upgrade routing
    2. Add upgrade event routing on httpsServer:
       - `/ws/listener` -> route to protoo/listener handler
       - All other paths -> route to existing admin ws WebSocketServer
    3. Expose httpsServer in ServerComponents so StreamingSubsystem can access it

    **Modify `sidecar/src/index.ts`:**
    1. Create StreamingSubsystem after AudioSubsystem
    2. Start streaming after server is ready: `await streamingSubsystem.start(components.httpsServer)`
    3. Update graceful shutdown order: streamingSubsystem -> audioSubsystem -> server
    4. Handle streamingSubsystem stop/restart on server restart
  </action>
  <verify>
    `npx tsc --noEmit -p sidecar/tsconfig.json` -- zero errors.
    index.ts references StreamingSubsystem in startup and shutdown.
    httpsServer upgrade handler routes /ws/listener differently from other paths.
  </verify>
  <done>StreamingSubsystem integrated into lifecycle. Shutdown order: mediasoup first, then GStreamer.</done>
</task>

<task type="auto">
  <name>Task 3: Add streaming admin WebSocket API messages</name>
  <files>sidecar/src/ws/handler.ts</files>
  <action>
    **Add streaming WebSocket API messages for admin dashboard:**
    - `"streaming:status"` -- Returns listener counts per channel, worker status, active streaming channels
    - `"streaming:workers"` -- Returns WorkerResourceInfo[] for worker monitoring display
    - `"streaming:listeners"` -- Returns ListenerSessionInfo[] for admin listener list
    - `"streaming:restart-workers"` -- Triggers worker restart (UI handles confirmation)

    **Add message prefix recognition:**
    - Create `STREAMING_MESSAGE_PREFIXES` set and `isStreamingMessageType()` check

    **Wire StreamingSubsystem events to admin broadcast:**
    - `streamingSubsystem.on("listener-count-changed")` -> broadcast to admin clients
    - `streamingSubsystem.on("worker-alert")` -> broadcast to admin clients
  </action>
  <verify>
    `npx tsc --noEmit -p sidecar/tsconfig.json` -- zero errors.
    handler.ts handles all 4 streaming:* message types.
  </verify>
  <done>Admin WebSocket exposes streaming status, listener list, worker info, and restart command.</done>
</task>

<task type="auto">
  <name>Task 4: Verify AudioSubsystem channel-state-changed event compatibility</name>
  <files>sidecar/src/audio/audio-subsystem.ts</files>
  <action>
    **Verify (no structural changes expected):**
    - Ensure `channel-state-changed` event emits channelId + status (sufficient for StreamingSubsystem)
    - Ensure `channel-removed` and `channel-created` events exist and include necessary data
    - If any event is missing or has insufficient data, add it

    **NOTE:** StreamingSubsystem subscribes to these events externally -- no changes to AudioSubsystem's internal structure.
  </action>
  <verify>
    AudioSubsystem emits channel-state-changed, channel-removed, channel-created events.
  </verify>
  <done>AudioSubsystem events confirmed compatible with StreamingSubsystem integration.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p sidecar/tsconfig.json` -- zero errors
2. index.ts creates and starts StreamingSubsystem
3. Shutdown order is: streamingSubsystem -> audioSubsystem -> server
4. httpsServer upgrade event routes /ws/listener to protoo
5. Admin WebSocket handles streaming:status, streaming:workers, streaming:listeners, streaming:restart-workers
6. Channel state changes in AudioSubsystem trigger streaming layer sync
</verification>

<success_criteria>
- End-to-end integration: GStreamer pipeline starts -> PlainTransport created -> Listener connects -> Consumer receives audio
- Graceful shutdown sends serverShuttingDown notification before disconnecting listeners
- Shutdown order matches locked decision: mediasoup first, then GStreamer
- Admin can query streaming status via existing WebSocket API
- Server restart properly stops and restarts streaming subsystem
</success_criteria>

<output>
After completion, create `.planning/phases/04-webrtc-streaming-core/04-05-SUMMARY.md`
</output>
