---
phase: 04-webrtc-streaming-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sidecar/src/utils/debounce.ts
  - sidecar/src/utils/error-message.ts
  - sidecar/src/audio/pipeline/pipeline-builder.ts
  - sidecar/src/audio/channels/channel-manager.ts
  - sidecar/src/audio/monitor/event-logger.ts
  - sidecar/src/audio/pipeline/pipeline-manager.ts
  - sidecar/src/audio/sources/source-registry.ts
autonomous: true

must_haves:
  truths:
    - "Debounce logic exists in exactly one place (utils/debounce.ts), not duplicated across 4 files"
    - "Error message narrowing exists in exactly one place (utils/error-message.ts), not duplicated across 5+ files"
    - "Channel selection pipeline string has a single builder function, not two near-identical functions"
  artifacts:
    - path: "sidecar/src/utils/debounce.ts"
      provides: "Reusable scheduleDebounced utility"
      exports: ["scheduleDebounced"]
    - path: "sidecar/src/utils/error-message.ts"
      provides: "Reusable toErrorMessage utility"
      exports: ["toErrorMessage"]
  key_links:
    - from: "sidecar/src/audio/channels/channel-manager.ts"
      to: "sidecar/src/utils/debounce.ts"
      via: "import scheduleDebounced"
      pattern: "scheduleDebounced"
    - from: "sidecar/src/audio/channels/channel-manager.ts"
      to: "sidecar/src/utils/error-message.ts"
      via: "import toErrorMessage"
      pattern: "toErrorMessage"
---

<objective>
Extract duplicated utility patterns identified in the pre-Phase 04 audit into shared modules, and consolidate the channel selection pipeline builder.

Purpose: Clean up DRY/SRP violations before Phase 4 adds new complexity. These utilities will also be used by new streaming code.
Output: Two new utility modules (debounce, error-message) and consolidated pipeline builder functions.
</objective>

<execution_context>
@C:\Users\rolan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\rolan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-webrtc-streaming-core/04-CONTEXT.md (audit-findings section)
@sidecar/src/utils/logger.ts
@sidecar/src/audio/channels/channel-manager.ts
@sidecar/src/audio/monitor/event-logger.ts
@sidecar/src/audio/pipeline/pipeline-manager.ts
@sidecar/src/audio/sources/source-registry.ts
@sidecar/src/audio/pipeline/pipeline-builder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract scheduleDebounced and toErrorMessage utilities</name>
  <files>
    sidecar/src/utils/debounce.ts
    sidecar/src/utils/error-message.ts
    sidecar/src/audio/channels/channel-manager.ts
    sidecar/src/audio/monitor/event-logger.ts
    sidecar/src/audio/pipeline/pipeline-manager.ts
    sidecar/src/audio/sources/source-registry.ts
  </files>
  <action>
    **Create `sidecar/src/utils/debounce.ts`:**
    - Export a generic `scheduleDebounced<T>()` function that manages a debounce timer keyed by a string identifier
    - Signature: `scheduleDebounced(timerMap: Map<string, NodeJS.Timeout>, key: string, delayMs: number, callback: () => void): void`
    - If a timer exists for `key`, clear it before setting a new one
    - Also export `clearDebounceTimer(timerMap: Map<string, NodeJS.Timeout>, key: string): void` for cleanup (addresses audit finding #4: timer clearing not extracted)
    - Also export `clearAllDebounceTimers(timerMap: Map<string, NodeJS.Timeout>): void` for shutdown

    **Create `sidecar/src/utils/error-message.ts`:**
    - Export `toErrorMessage(err: unknown): string` that returns `err instanceof Error ? err.message : String(err)`
    - Single-line utility, replaces ~5 inline occurrences across the codebase

    **Replace inline debounce patterns in these 4 files:**
    1. `channel-manager.ts` -- uses setTimeout for processing config debounced restart. Replace with `scheduleDebounced` import. Also replace the timer clearing with `clearDebounceTimer`.
    2. `event-logger.ts` -- uses setTimeout for debounced flush. Replace with `scheduleDebounced`. Also replace manual `clearTimeout` in `clearChannel` and `scheduleDebouncedFlush` with `clearDebounceTimer`/`clearAllDebounceTimers` (audit finding #4).
    3. `pipeline-manager.ts` -- uses setTimeout for restart delay. Replace with `scheduleDebounced` if pattern matches (check if it's actually debounce or just a delayed action; if it's a one-shot delay, leave it).
    4. `source-registry.ts` -- uses setTimeout for debounced persistence. Replace with `scheduleDebounced`.

    **Replace inline error narrowing in all files where `err instanceof Error ? err.message : String(err)` appears:**
    - Search all .ts files in sidecar/src/ for this pattern
    - Replace each occurrence with `toErrorMessage(err)` and add the import

    **IMPORTANT:** Preserve all existing behavior exactly. Only extract to shared functions -- no logic changes.
  </action>
  <verify>
    Run `npx tsx --eval "import { scheduleDebounced, clearDebounceTimer, clearAllDebounceTimers } from './sidecar/src/utils/debounce.js'; console.log('debounce OK');"` from project root to verify module loads.
    Run `npx tsx --eval "import { toErrorMessage } from './sidecar/src/utils/error-message.js'; console.log(toErrorMessage(new Error('test')), toErrorMessage('string'));"` to verify error-message utility.
    Search for remaining inline `err instanceof Error ? err.message : String(err)` patterns -- should be zero or near-zero.
    Search for remaining inline setTimeout-based debounce patterns in the 4 target files -- should use scheduleDebounced instead.
  </verify>
  <done>
    scheduleDebounced utility used by 3-4 files instead of inline setTimeout patterns. toErrorMessage used across codebase instead of inline ternary. clearDebounceTimer extracted for event-logger timer cleanup.
  </done>
</task>

<task type="auto">
  <name>Task 2: Consolidate channel selection pipeline builder and normalize channel data mapping</name>
  <files>
    sidecar/src/audio/pipeline/pipeline-builder.ts
    sidecar/src/audio/channels/channel-manager.ts
  </files>
  <action>
    **Consolidate channel selection in `pipeline-builder.ts` (audit finding #3):**
    - Currently `buildChannelSelection` (for AES67) and `buildChannelSelectionForLocal` are nearly identical for mono/stereo cases
    - Consolidate into a single `buildChannelSelectionString(selectedChannels: number[], outputFormat: 'mono' | 'stereo', totalSourceChannels?: number): string`
    - The `totalSourceChannels` parameter is optional -- when undefined (local devices), use the format-based logic; when provided (AES67), use channel-count-aware logic
    - Update all call sites in pipeline-builder.ts to use the consolidated function
    - Remove the old separate functions

    **Normalize channel data mapping in `channel-manager.ts` (audit finding #2):**
    - Identify the duplicated object mapping between load and persist operations (the code that maps channel data between config format and runtime format)
    - Extract `normalizeSourceAssignment()` and `normalizeProcessingConfig()` helper functions within channel-manager.ts (private helpers, not exported -- they're channel-manager-specific)
    - Replace both the load and persist mapping code with calls to these helpers

    **IMPORTANT:** Preserve all existing behavior exactly. Only consolidate -- no logic changes.
  </action>
  <verify>
    Search pipeline-builder.ts for `buildChannelSelectionForLocal` -- should not exist (consolidated into single function).
    Search channel-manager.ts for the old duplicated mapping pattern -- should now use helper functions.
    Run `npx tsc --noEmit -p sidecar/tsconfig.json` to verify no type errors.
  </verify>
  <done>
    Channel selection has a single builder function. Channel data normalization has extracted helpers. TypeScript compilation succeeds with no errors.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p sidecar/tsconfig.json` -- zero errors
2. No remaining duplicated debounce patterns (search: `setTimeout.*debounce` or similar)
3. No remaining `buildChannelSelectionForLocal` function
4. `toErrorMessage` imported and used in at least 3 files
5. `scheduleDebounced` imported and used in at least 3 files
</verification>

<success_criteria>
- All 5 audit DRY/SRP findings addressed
- Zero TypeScript compilation errors
- All existing audio pipeline behavior preserved (no logic changes)
</success_criteria>

<output>
After completion, create `.planning/phases/04-webrtc-streaming-core/04-01-SUMMARY.md`
</output>
