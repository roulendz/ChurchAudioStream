---
phase: 04-webrtc-streaming-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sidecar/src/utils/debounce.ts
  - sidecar/src/utils/error-message.ts
  - sidecar/src/audio/pipeline/pipeline-builder.ts
  - sidecar/src/audio/channels/channel-manager.ts
  - sidecar/src/audio/monitor/event-logger.ts
  - sidecar/src/audio/pipeline/pipeline-manager.ts
  - sidecar/src/audio/sources/source-registry.ts
autonomous: true

must_haves:
  truths:
    - "Debounce logic exists in exactly one place (utils/debounce.ts), not duplicated across 4 files"
    - "Error message narrowing exists in exactly one place (utils/error-message.ts), not duplicated across 5+ files"
    - "Channel selection pipeline string has a single builder function, not two near-identical functions"
  artifacts:
    - path: "sidecar/src/utils/debounce.ts"
      provides: "Reusable scheduleDebounced utility"
      exports: ["scheduleDebounced"]
    - path: "sidecar/src/utils/error-message.ts"
      provides: "Reusable toErrorMessage utility"
      exports: ["toErrorMessage"]
  key_links:
    - from: "sidecar/src/audio/channels/channel-manager.ts"
      to: "sidecar/src/utils/debounce.ts"
      via: "import scheduleDebounced"
      pattern: "scheduleDebounced"
    - from: "sidecar/src/audio/channels/channel-manager.ts"
      to: "sidecar/src/utils/error-message.ts"
      via: "import toErrorMessage"
      pattern: "toErrorMessage"
---

<objective>
Extract duplicated utility patterns identified in the pre-Phase 04 audit into shared modules, and consolidate the channel selection pipeline builder.

Purpose: Clean up DRY/SRP violations before Phase 4 adds new complexity. These utilities will also be used by new streaming code.
Output: Two new utility modules (debounce, error-message) and consolidated pipeline builder functions.
</objective>

<execution_context>
@C:\Users\rolan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\rolan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-webrtc-streaming-core/04-AUDIT.md (DRY/SRP findings)
@sidecar/src/utils/logger.ts
@sidecar/src/audio/channels/channel-manager.ts
@sidecar/src/audio/monitor/event-logger.ts
@sidecar/src/audio/pipeline/pipeline-manager.ts
@sidecar/src/audio/sources/source-registry.ts
@sidecar/src/audio/pipeline/pipeline-builder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract scheduleDebounced utility and replace inline patterns</name>
  <files>
    sidecar/src/utils/debounce.ts
    sidecar/src/audio/channels/channel-manager.ts
    sidecar/src/audio/monitor/event-logger.ts
    sidecar/src/audio/pipeline/pipeline-manager.ts
    sidecar/src/audio/sources/source-registry.ts
  </files>
  <action>
    **Create `sidecar/src/utils/debounce.ts`:**
    - Export `scheduleDebounced(timerMap: Map<string, NodeJS.Timeout>, key: string, delayMs: number, callback: () => void): void`
    - If a timer exists for `key`, clear it before setting a new one
    - Export `clearDebounceTimer(timerMap: Map<string, NodeJS.Timeout>, key: string): void` for cleanup (audit finding #4)
    - Export `clearAllDebounceTimers(timerMap: Map<string, NodeJS.Timeout>): void` for shutdown

    **Replace inline debounce patterns in these 4 files:**
    1. `channel-manager.ts` -- replace setTimeout debounce + timer clearing with `scheduleDebounced`/`clearDebounceTimer`
    2. `event-logger.ts` -- replace setTimeout debounce + manual `clearTimeout` with `scheduleDebounced`/`clearDebounceTimer`/`clearAllDebounceTimers`
    3. `pipeline-manager.ts` -- replace with `scheduleDebounced` if pattern is debounce (leave one-shot delays as-is)
    4. `source-registry.ts` -- replace setTimeout debounce with `scheduleDebounced`

    **IMPORTANT:** Preserve all existing behavior exactly. Only extract to shared functions -- no logic changes.
  </action>
  <verify>
    `npx tsx --eval "import { scheduleDebounced, clearDebounceTimer, clearAllDebounceTimers } from './sidecar/src/utils/debounce.js'; console.log('debounce OK');"` loads successfully.
    Search for remaining inline setTimeout-based debounce patterns in the 4 target files -- should be zero.
  </verify>
  <done>scheduleDebounced utility used by 3-4 files instead of inline setTimeout patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Extract toErrorMessage utility and replace inline patterns</name>
  <files>
    sidecar/src/utils/error-message.ts
    sidecar/src/audio/channels/channel-manager.ts
    sidecar/src/audio/monitor/event-logger.ts
    sidecar/src/audio/pipeline/pipeline-manager.ts
    sidecar/src/audio/sources/source-registry.ts
  </files>
  <action>
    **Create `sidecar/src/utils/error-message.ts`:**
    - Export `toErrorMessage(err: unknown): string` that returns `err instanceof Error ? err.message : String(err)`

    **Replace inline error narrowing in all files where `err instanceof Error ? err.message : String(err)` appears:**
    - Search all .ts files in sidecar/src/ for this pattern
    - Replace each occurrence with `toErrorMessage(err)` and add the import

    **IMPORTANT:** Preserve all existing behavior exactly. Only extract -- no logic changes.
  </action>
  <verify>
    `npx tsx --eval "import { toErrorMessage } from './sidecar/src/utils/error-message.js'; console.log(toErrorMessage(new Error('test')), toErrorMessage('string'));"` loads and outputs correctly.
    Search for remaining `err instanceof Error ? err.message : String(err)` patterns -- must be zero.
  </verify>
  <done>toErrorMessage used across codebase instead of inline ternary.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate channel selection pipeline builder</name>
  <files>sidecar/src/audio/pipeline/pipeline-builder.ts</files>
  <action>
    **Consolidate channel selection in `pipeline-builder.ts` (audit finding #3):**
    - Currently `buildChannelSelection` (for AES67) and `buildChannelSelectionForLocal` are nearly identical for mono/stereo cases
    - Consolidate into a single `buildChannelSelectionString(selectedChannels: number[], outputFormat: 'mono' | 'stereo', totalSourceChannels?: number): string`
    - The `totalSourceChannels` parameter is optional -- when undefined (local devices), use the format-based logic; when provided (AES67), use channel-count-aware logic
    - Update all call sites in pipeline-builder.ts to use the consolidated function
    - Remove the old separate functions

    **IMPORTANT:** Preserve all existing behavior exactly. Only consolidate -- no logic changes.
  </action>
  <verify>
    Search pipeline-builder.ts for `buildChannelSelectionForLocal` -- must not exist (consolidated into single function).
    `npx tsc --noEmit -p sidecar/tsconfig.json` -- zero errors.
  </verify>
  <done>Channel selection has a single builder function.</done>
</task>

<task type="auto">
  <name>Task 4: Normalize channel data mapping helpers</name>
  <files>sidecar/src/audio/channels/channel-manager.ts</files>
  <action>
    **Normalize channel data mapping in `channel-manager.ts` (audit finding #2):**
    - Identify the duplicated object mapping between load and persist operations
    - Extract `normalizeSourceAssignment()` and `normalizeProcessingConfig()` private helpers
    - Replace both the load and persist mapping code with calls to these helpers

    **IMPORTANT:** Preserve all existing behavior exactly. Only consolidate -- no logic changes.
  </action>
  <verify>
    Search channel-manager.ts for the old duplicated mapping pattern -- should now use helper functions.
    `npx tsc --noEmit -p sidecar/tsconfig.json` -- zero errors.
  </verify>
  <done>Channel data normalization uses extracted helpers. TypeScript compilation succeeds.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p sidecar/tsconfig.json` -- zero errors
2. No remaining inline setTimeout-based debounce patterns in target files
3. No remaining `err instanceof Error ? err.message : String(err)` patterns
4. No remaining `buildChannelSelectionForLocal` function
5. `toErrorMessage` imported and used in at least 3 files
6. `scheduleDebounced` imported and used in at least 3 files
</verification>

<success_criteria>
- All 5 audit DRY/SRP findings addressed
- Zero TypeScript compilation errors
- All existing audio pipeline behavior preserved (no logic changes)
</success_criteria>

<output>
After completion, create `.planning/phases/04-webrtc-streaming-core/04-01-SUMMARY.md`
</output>
