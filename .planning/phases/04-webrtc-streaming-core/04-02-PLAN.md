---
phase: 04-webrtc-streaming-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sidecar/package.json
  - sidecar/src/config/schema.ts
  - sidecar/src/streaming/streaming-types.ts
autonomous: true

must_haves:
  truths:
    - "mediasoup and protoo-server are installed as dependencies"
    - "Config schema includes mediasoup section with workerCount, rtcMinPort, rtcMaxPort, logLevel"
    - "Streaming type definitions cover all mediasoup object lifecycle states"
  artifacts:
    - path: "sidecar/src/streaming/streaming-types.ts"
      provides: "Type definitions for streaming domain"
      exports: ["ListenerPeerData", "ChannelStreamingState", "WorkerState", "StreamingConfig", "LatencyMode", "LossRecoveryMode"]
    - path: "sidecar/src/config/schema.ts"
      provides: "MediasoupSchema added to ConfigSchema"
      contains: "MediasoupSchema"
  key_links:
    - from: "sidecar/src/config/schema.ts"
      to: "sidecar/src/streaming/streaming-types.ts"
      via: "Config types used by streaming modules"
      pattern: "mediasoup"
---

<objective>
Install mediasoup and protoo-server dependencies, extend the config schema with mediasoup settings, and define all streaming type definitions needed by Phase 4.

Purpose: Establish the type system and configuration foundation for all subsequent streaming plans.
Output: New dependencies installed, config schema extended, streaming-types.ts with complete type definitions.
</objective>

<execution_context>
@C:\Users\rolan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\rolan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-webrtc-streaming-core/04-CONTEXT.md
@.planning/phases/04-webrtc-streaming-core/04-RESEARCH.md
@sidecar/package.json
@sidecar/src/config/schema.ts
@sidecar/src/audio/processing/processing-types.ts
@sidecar/src/audio/channels/channel-types.ts
@sidecar/src/audio/processing/port-allocator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install mediasoup and protoo-server dependencies</name>
  <files>sidecar/package.json</files>
  <action>
    Run from the `sidecar/` directory:
    ```
    npm install mediasoup@^3.19 protoo-server@^4.0
    ```

    **Build note:** mediasoup compiles a C++ worker binary via node-gyp at install time. This requires:
    - Python 3 (already available on most systems)
    - C++ compiler (MSVC via windows-build-tools on Windows, or gcc/clang on Linux/Mac)
    - If the install fails due to missing build tools, install them: `npm install --global windows-build-tools` (Windows) or install build-essential (Linux)

    After install, verify:
    - `node -e "require('mediasoup')"` exits cleanly (worker binary compiled)
    - `node -e "require('protoo-server')"` exits cleanly

    Also install the types for mediasoup (mediasoup ships its own types, no @types package needed). protoo-server does NOT have @types -- we will create a minimal type declaration in the streaming-types.ts.
  </action>
  <verify>
    `cd sidecar && node -e "const ms = require('mediasoup'); console.log('mediasoup version:', ms.version);"` prints version.
    `cd sidecar && node -e "require('protoo-server'); console.log('protoo-server OK');"` exits cleanly.
    package.json contains `mediasoup` and `protoo-server` in dependencies.
  </verify>
  <done>mediasoup 3.19.x and protoo-server 4.0.x installed with working C++ worker binary.</done>
</task>

<task type="auto">
  <name>Task 2: Extend config schema with mediasoup and streaming settings</name>
  <files>sidecar/src/config/schema.ts</files>
  <action>
    Add `MediasoupSchema` with fields (per research discretion recommendations):
    - `workerCount`: z.number().int().min(1).max(8).default(1)
    - `rtcMinPort`: z.number().int().min(1024).max(65535).default(40000)
    - `rtcMaxPort`: z.number().int().min(1024).max(65535).default(49999)
    - `logLevel`: z.enum(["debug", "warn", "error", "none"]).default("warn")

    Add `StreamingSchema` with fields (per locked decisions):
    - `heartbeatIntervalMs`: z.number().int().min(10000).max(120000).default(30000)
    - `rateLimitPerIp`: z.number().int().min(1).max(50).default(5)
    - `rateLimitWindowMs`: z.number().int().min(1000).max(60000).default(10000)
    - `shutdownDrainMs`: z.number().int().min(1000).max(30000).default(5000)

    Add per-channel fields to existing `ChannelSchema`:
    - `latencyMode`: z.enum(["live", "stable"]).default("live")
    - `lossRecovery`: z.enum(["nack", "plc"]).default("nack")
    - `defaultChannel`: z.boolean().default(false)

    Wire `mediasoup` and `streaming` into ConfigSchema with `.default(...)`.
  </action>
  <verify>
    `npx tsc --noEmit -p sidecar/tsconfig.json` -- zero errors.
    Config parses with defaults: `npx tsx --eval "import { ConfigSchema } from './sidecar/src/config/schema.js'; const c = ConfigSchema.parse({}); console.log(c.mediasoup, c.streaming);"`.
  </verify>
  <done>Config schema extended with mediasoup, streaming, and per-channel settings with backward-compatible defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Create streaming type definitions</name>
  <files>sidecar/src/streaming/streaming-types.ts</files>
  <action>
    Create `sidecar/src/streaming/streaming-types.ts` with all streaming domain types.

    Types to define (see 04-RESEARCH.md Code Examples for reference patterns):
    - `LatencyMode`, `LossRecoveryMode` -- enums matching config schema
    - `WorkerState` -- per-worker tracking (worker, index, memoryMonitorInterval)
    - `ChannelStreamingState` -- per-channel (channelId, router, plainTransport, audioProducer)
    - `ListenerPeerData` -- per-peer (sessionId, connectedAt, rtpCapabilities, webRtcTransport, currentConsumer, currentChannelId, isAdmin)
    - `ListenerSessionInfo` -- admin dashboard subset of peer data
    - `ListenerChannelInfo` -- channel info pushed to listeners
    - `LatencyEstimate` -- component-based latency breakdown
    - `WorkerResourceInfo` -- worker resource snapshot for admin
    - `OPUS_RTP_CODEC` -- const matching Phase 3 pipeline builder codec
    - `OPUS_PAYLOAD_TYPE` -- const 101 matching rtpopuspay pt=101

    Also: check if protoo-server ships its own types. If not, create `sidecar/src/streaming/protoo-types.d.ts` with minimal declarations for `WebSocketServer`, `Room`, `Peer`, and event signatures.
  </action>
  <verify>
    `npx tsc --noEmit -p sidecar/tsconfig.json` -- zero errors.
    Types importable: `npx tsx --eval "import { OPUS_RTP_CODEC, OPUS_PAYLOAD_TYPE } from './sidecar/src/streaming/streaming-types.js'; console.log(OPUS_RTP_CODEC, OPUS_PAYLOAD_TYPE);"`.
  </verify>
  <done>All streaming types defined and importable. Protoo type declarations created if needed.</done>
</task>

</tasks>

<verification>
1. `cd sidecar && npm ls mediasoup protoo-server` -- both listed as direct dependencies
2. `npx tsc --noEmit -p sidecar/tsconfig.json` -- zero errors
3. Config parses with mediasoup and streaming defaults (backward-compatible)
4. streaming-types.ts exports all required types and consts
5. Per-channel latencyMode/lossRecovery/defaultChannel in ChannelSchema
</verification>

<success_criteria>
- mediasoup C++ worker binary compiles successfully on the build machine
- Config schema backward-compatible (existing configs without mediasoup/streaming sections get defaults)
- All streaming types defined matching research patterns
</success_criteria>

<output>
After completion, create `.planning/phases/04-webrtc-streaming-core/04-02-SUMMARY.md`
</output>
