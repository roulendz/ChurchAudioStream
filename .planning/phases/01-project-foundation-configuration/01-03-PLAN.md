---
phase: 01-project-foundation-configuration
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - sidecar/src/index.ts
  - sidecar/src/server.ts
  - sidecar/src/ws/handler.ts
  - src/App.tsx
  - src/App.css
  - src/hooks/useWebSocket.ts
  - src/hooks/useServerStatus.ts
  - src/components/ConnectionStatus.tsx
  - src/components/SettingsPanel.tsx
  - src/components/LogViewer.tsx
autonomous: false

must_haves:
  truths:
    - "Admin can change the web server port in settings, click Save, and the server restarts on the new port"
    - "Admin can change the network interface to bind to in settings"
    - "Changed settings persist after closing and reopening the app"
    - "Connection status indicator shows connected/disconnected/reconnecting state in real time"
    - "Admin can view sidecar log output in the admin UI"
  artifacts:
    - path: "src/hooks/useWebSocket.ts"
      provides: "React hook managing WebSocket connection with auto-reconnect"
      exports: ["useWebSocket"]
      contains: "WebSocket"
    - path: "src/hooks/useServerStatus.ts"
      provides: "React hook for server status and config state"
      exports: ["useServerStatus"]
    - path: "src/components/ConnectionStatus.tsx"
      provides: "Always-visible connection status indicator"
      exports: ["ConnectionStatus"]
    - path: "src/components/SettingsPanel.tsx"
      provides: "Settings form with port, interface, domain fields and Save button"
      exports: ["SettingsPanel"]
      contains: "Save"
    - path: "src/components/LogViewer.tsx"
      provides: "Scrollable log viewer showing sidecar output"
      exports: ["LogViewer"]
  key_links:
    - from: "src/hooks/useWebSocket.ts"
      to: "sidecar WebSocket endpoint"
      via: "wss://localhost:{port} connection"
      pattern: "new WebSocket.*wss://"
    - from: "src/components/SettingsPanel.tsx"
      to: "src/hooks/useWebSocket.ts"
      via: "sendMessage with config:update type"
      pattern: "config:update"
    - from: "sidecar/src/ws/handler.ts"
      to: "sidecar/src/server.ts"
      via: "server restart triggered by config:update with port/interface change"
      pattern: "restartServer|requiresRestart"
    - from: "sidecar/src/index.ts"
      to: "sidecar/src/server.ts"
      via: "restart orchestration (stop old server, start new)"
      pattern: "stopServer.*startServer"
---

<objective>
Wire config changes to server restarts and build the minimal admin UI shell with connection status, settings panel, and log viewer. This completes the Phase 1 loop: admin changes settings in the UI -> config updates via WebSocket -> server restarts on new address -> config persists to disk.

Purpose: This is the final integration plan that proves the entire Phase 1 skeleton works end-to-end. The admin UI shell is minimal (not the full dashboard from Phase 6) but sufficient to verify all five success criteria.
Output: A working admin UI with connection status, settings management, and log viewing -- all communicating with the sidecar via WebSocket.
</objective>

<execution_context>
@C:\Users\rolan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\rolan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\laragon\www\ChurchAudioStream\.planning\PROJECT.md
@C:\laragon\www\ChurchAudioStream\.planning\ROADMAP.md
@C:\laragon\www\ChurchAudioStream\.planning\phases\01-project-foundation-configuration\01-RESEARCH.md
@C:\laragon\www\ChurchAudioStream\.planning\phases\01-project-foundation-configuration\01-CONTEXT.md
@C:\laragon\www\ChurchAudioStream\.planning\phases\01-project-foundation-configuration\01-01-SUMMARY.md
@C:\laragon\www\ChurchAudioStream\.planning\phases\01-project-foundation-configuration\01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement server restart on config change and broadcast</name>
  <files>
    sidecar/src/index.ts
    sidecar/src/server.ts
    sidecar/src/ws/handler.ts
  </files>
  <action>
    **Server restart logic (`sidecar/src/server.ts`):**
    - Add an exported `restartServer` function that:
      1. Sends "server:restarting" message to all connected WS clients with the new host/port info
      2. Closes all WebSocket connections with close code 1012 ("Service Restart")
      3. Calls the stopServer function (closes HTTPS server, unpublishes mDNS, waits for 'close' event)
      4. Waits 500ms for port release (Pitfall 6: TCP TIME_WAIT)
      5. Creates a NEW HTTPS server with the updated config (re-reads cert, creates new Express app)
      6. Creates a NEW WebSocketServer attached to the new HTTPS server
      7. Calls startServer with new config
      8. Returns the new server/wss instances
    - This function needs access to the ConfigStore to re-read current config after update
    - Handle EADDRINUSE: if the new port is occupied, log error, attempt to restart on the OLD port as fallback, notify clients of failure

    **Wire config:update to restart (`sidecar/src/ws/handler.ts`):**
    - When handling "config:update" message:
      1. Call configStore.update(payload) to validate and persist
      2. If update fails validation, respond with error and validation messages
      3. If update succeeds, check if port, host, or interface changed (compare old vs new config)
      4. If server-restart fields changed: set a `requiresRestart: true` flag in the response
      5. Broadcast "config:updated" to all admin clients with the new config and requiresRestart flag
      6. If requiresRestart is true, schedule the restart with a 1-second delay (so the response reaches clients first):
         - Call restartServer via a callback/event pattern (handler should not own server lifecycle directly)
         - Use an EventEmitter or callback pattern: handler emits "restart-needed" event, index.ts listens and orchestrates

    **Orchestration (`sidecar/src/index.ts`):**
    - After creating the server and WebSocket handler, listen for "restart-needed" events
    - On restart-needed:
      1. Log "Server restart requested due to config change"
      2. Call restartServer with the updated config
      3. Re-register the event listener on the new handler (since WSS is recreated)
      4. Log "Server restarted on new address"
    - Ensure the startup flow is refactored so that "create and start server" is a reusable function (called both on initial boot and on restart)

    **Add "interfaces:list" WebSocket message type:**
    - In handler.ts, add handler for "interfaces:list" message:
      - Calls listNetworkInterfaces()
      - Returns array of available interfaces with name and address
      - Available to all roles (admin and listener may both need this for diagnostics)

    IMPORTANT: Server restart must be sequential (close old, wait, start new). Never have two servers trying to bind simultaneously.
    IMPORTANT: After restart, all clients will need to reconnect. The admin UI handles this via auto-reconnect (Task 2).
    IMPORTANT: The WebSocket handler should NOT directly import or call server functions. Use an event/callback pattern to keep separation of concerns (SRP).
  </action>
  <verify>
    - Start sidecar: `cd sidecar && npx tsx src/index.ts -- --config-path .`
    - Connect with wscat: `wscat -c wss://localhost:7777 --no-check`
    - Send identify as admin: `{"type":"identify","payload":{"role":"admin"}}`
    - Send config:update to change port: `{"type":"config:update","payload":{"server":{"port":8888}},"requestId":"r1"}`
    - Verify response includes `requiresRestart: true`
    - Verify connection closes with code 1012
    - Wait 2 seconds, then connect to new port: `wscat -c wss://localhost:8888 --no-check`
    - Verify server is running on port 8888
    - Check config.json on disk -- port should be 8888
    - Send config:update to change back to 7777, verify restart works again
    - Request interfaces:list: `{"type":"interfaces:list","requestId":"r2"}` -- get list of network interfaces
  </verify>
  <done>Config:update that changes port/host/interface triggers a graceful server restart (notify clients -> close -> wait -> rebind). Config persists to disk. Server successfully restarts on the new address. EADDRINUSE is handled with fallback. Interfaces are listable via WebSocket.</done>
</task>

<task type="auto">
  <name>Task 2: Build React admin UI shell with WebSocket hooks and settings panel</name>
  <files>
    src/hooks/useWebSocket.ts
    src/hooks/useServerStatus.ts
    src/components/ConnectionStatus.tsx
    src/components/SettingsPanel.tsx
    src/components/LogViewer.tsx
    src/App.tsx
    src/App.css
  </files>
  <action>
    **useWebSocket hook (`src/hooks/useWebSocket.ts`):**
    - Manages a single WebSocket connection to the sidecar
    - Constructor: takes URL (wss://localhost:{port})
    - Auto-reconnect with exponential backoff: 1s, 2s, 4s, 8s, max 30s
    - On connect: send "identify" message with role "admin"
    - Expose:
      - `status`: 'connecting' | 'connected' | 'disconnected' | 'reconnecting'
      - `sendMessage(type, payload?, requestId?)`: send a typed WsMessage
      - `subscribe(type, callback)`: register handler for specific message types
      - `lastMessage`: most recent message received (for debugging)
    - On "server:restarting" message: note the new port from payload, update reconnect URL
    - On close with code 1012: immediately start reconnect (server restart, not error)
    - On unexpected close: start exponential backoff reconnect
    - Cleanup on unmount: close WebSocket, cancel reconnect timers

    **useServerStatus hook (`src/hooks/useServerStatus.ts`):**
    - Uses useWebSocket internally
    - On connect, sends "config:get" to load current config
    - Subscribes to "config:updated" for live updates
    - Subscribes to "server:status" for status info
    - Exposes:
      - `config`: current AppConfig (or null if not loaded)
      - `connectionStatus`: from useWebSocket
      - `updateConfig(partial)`: sends config:update message, returns promise that resolves with result
      - `interfaces`: list of available network interfaces (fetched on mount via "interfaces:list")

    **ConnectionStatus component (`src/components/ConnectionStatus.tsx`):**
    - Always visible at the top of the admin UI (sticky/fixed position)
    - Shows colored indicator dot + text:
      - Green dot + "Connected" when connected
      - Yellow dot + "Reconnecting..." when reconnecting (with attempt count)
      - Red dot + "Disconnected" when disconnected
    - Compact design -- should not take much vertical space
    - This fulfills the user's requirement: "Always-visible connection status indicator"

    **SettingsPanel component (`src/components/SettingsPanel.tsx`):**
    - Shows current server settings in a form:
      - Port: number input (1024-65535)
      - Network Interface: dropdown populated from interfaces list (show "name (address)" format)
      - mDNS Enabled: toggle/checkbox
      - mDNS Domain: text input (e.g., "churchaudio.local")
    - Save/Apply button workflow:
      - Form starts in "clean" state matching server config
      - Editing any field marks the form as "dirty" (Save button becomes active)
      - Clicking Save sends config:update via WebSocket
      - If response has requiresRestart, show a brief "Server restarting..." message
      - On success: show "Settings saved" confirmation
      - On validation error: show error messages next to relevant fields
    - DO NOT make this a full settings page -- just the Phase 1 config fields. Phase 6 adds the full dashboard.
    - Use controlled inputs with local state, only send to server on Save

    **LogViewer component (`src/components/LogViewer.tsx`):**
    - Listens to Tauri "sidecar-log" and "sidecar-error" events (from the Tauri event system)
    - Import `listen` from `@tauri-apps/api/event`
    - Displays log lines in a scrollable container (max height, auto-scroll to bottom)
    - Color-code by level: info=white, warn=yellow, error=red
    - Parse the JSON log format from the sidecar: extract level, timestamp, message
    - Keep a rolling buffer of last 200 log lines (prevent memory growth during long sessions)
    - Show a "Clear" button to empty the log viewer
    - This fulfills the user's requirement: "Sidecar logs are viewable in a dedicated log viewer panel"
    - IMPORTANT: Also listen for WebSocket messages for when running in browser mode (not Tauri). Check if Tauri API is available; if not, fall back to subscribing to a "logs:subscribe" WS message type.

    **App.tsx and App.css:**
    - Simple layout with three sections stacked vertically:
      1. ConnectionStatus (sticky top bar)
      2. SettingsPanel (main content area)
      3. LogViewer (bottom panel, collapsible)
    - Title: "Church Audio Stream - Admin"
    - Clean, minimal styling with CSS (not a UI framework -- keep Phase 1 lightweight)
    - Dark background, light text (matches the church-friendly aesthetic from the placeholder page)
    - Responsive enough to look decent on both the Tauri desktop window (1200x800) and a browser

    IMPORTANT: The admin GUI connects via WebSocket like any browser client (NOT via Tauri IPC). Check useWebSocket connects to wss://localhost:{port}, not a Tauri command.
    IMPORTANT: When running in Tauri, the initial port is known (7777 default). When running in browser, read port from the current URL.
    IMPORTANT: Handle the self-signed cert issue in Tauri webview. If wss:// connection fails due to cert, try detecting the error and displaying a message about trusting the certificate. See research Open Question 3.
  </action>
  <verify>
    - Build the sidecar: `cd sidecar && npm run build`
    - Run `npm run tauri dev` to launch the full app
    - Verify:
      1. ConnectionStatus shows green "Connected" after startup
      2. SettingsPanel shows current config (port 7777, default interface, mDNS domain)
      3. Change port to 9999, click Save -- see "Server restarting..." then reconnection
      4. After reconnection, verify port is 9999 in settings
      5. LogViewer shows sidecar startup logs and restart logs
      6. Close app, reopen -- port is still 9999 (persisted)
      7. Kill sidecar manually -- ConnectionStatus turns red/yellow, then green when sidecar auto-restarts
    - Also test in browser: open `https://localhost:7777/admin` -- should work the same (except LogViewer uses WS instead of Tauri events)
  </verify>
  <done>React admin UI shell with always-visible connection status indicator, settings panel for port/interface/mDNS configuration with Save/Apply workflow, and scrollable log viewer. Admin can change server port, server restarts, config persists. All communication via WebSocket (admin GUI is just another client).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Phase 1 end-to-end on desktop and mobile</name>
  <what-built>
    Complete Phase 1 skeleton: Tauri 2.x desktop app with Node.js sidecar, Express HTTPS server on port 7777, WebSocket signaling, JSON config persistence, and admin UI with settings management.
  </what-built>
  <how-to-verify>
    **Desktop verification:**
    1. Run `npm run tauri dev` -- Tauri window opens with admin UI
    2. Verify ConnectionStatus shows green "Connected"
    3. Verify SettingsPanel shows port 7777 and available network interfaces
    4. Change port to 8888, click Save -- server restarts, UI reconnects
    5. Check LogViewer shows restart logs
    6. Close the app completely, reopen -- port should still be 8888 (persisted)
    7. Kill the sidecar process in Task Manager -- it should auto-restart within 2 seconds
    8. Close the Tauri window -- verify no orphaned Node.js processes remain in Task Manager

    **Mobile/Phone verification:**
    9. On your phone, connect to the same WiFi as the computer
    10. Open browser and navigate to `https://{computer-IP}:8888`
    11. Accept the self-signed certificate warning
    12. Verify the placeholder page loads ("Church Audio Stream - Listener web app coming soon")
    13. Verify the WebSocket connection status on the placeholder page shows "Connected"

    **Config corruption test:**
    14. Stop the app
    15. Edit config.json next to the executable -- put invalid JSON (e.g., just "broken")
    16. Start the app -- it should start with defaults and show a warning in logs
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which checks failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 Success Criteria (from ROADMAP.md):
1. Tauri desktop window launches on Windows (Mac/Linux untested until Phase 10) with Node.js sidecar running -- VERIFIED by Task 3 steps 1-2
2. Express web server serves a placeholder page at configured IP:port, accessible from phone -- VERIFIED by Task 3 steps 10-12
3. WebSocket signaling endpoint accepts connections from browser clients -- VERIFIED by Task 3 step 13
4. Changing a setting persists after restart -- VERIFIED by Task 3 steps 4-6
5. Admin can change web server IP and port, server restarts on new address -- VERIFIED by Task 3 step 4
</verification>

<success_criteria>
- Changing port in admin UI triggers graceful server restart on new port
- Changing network interface in admin UI updates server binding
- All config changes persist to config.json on disk and survive app restart
- Connection status indicator accurately reflects WebSocket connection state in real time
- Log viewer displays sidecar output with level-based color coding
- Server restart notifies all connected clients before closing
- Auto-reconnect recovers connection after server restart within 5 seconds
- Placeholder page is accessible from a phone browser on the same WiFi
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-configuration/01-03-SUMMARY.md`
</output>
