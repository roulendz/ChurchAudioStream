---
phase: 11-foundation
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/components/monitoring/ServerStatus.tsx
  - src/components/SettingsPanel.tsx
  - src/components/LogViewer.tsx
  - src/components/CheckForUpdatesButton/CheckForUpdatesButton.tsx
  - src/components/UpdateToast/UpdateToast.tsx
  - src/components/CheckForUpdatesButton/CheckForUpdatesButton.module.css
  - src/components/UpdateToast/UpdateToast.module.css
  - src/components/CheckForUpdatesButton/CheckForUpdatesButton.test.tsx
  - src/components/UpdateToast/UpdateToast.test.tsx
autonomous: true
requirements: [FOUN-03, TYPO-01]

must_haves:
  truths:
    - "ServerStatus stat cards render with shadcn card styling and conditional worker dots"
    - "SettingsPanel form fields use Tailwind input/label patterns with error states"
    - "SettingsPanel contains a Design Tokens section showing color swatches"
    - "LogViewer renders log entries with Tailwind-styled level badges"
    - "CheckForUpdatesButton uses inline Tailwind classes (CSS module file deleted)"
    - "UpdateToast uses inline Tailwind classes with slide animation (CSS module file deleted)"
    - "Existing component tests still pass after migration"
  artifacts:
    - path: "src/components/monitoring/ServerStatus.tsx"
      provides: "Stat cards with Tailwind styling"
      contains: "bg-card border border-border"
    - path: "src/components/SettingsPanel.tsx"
      provides: "Settings form + Design Tokens section"
      contains: "Design Tokens"
    - path: "src/components/LogViewer.tsx"
      provides: "Log viewer with Tailwind styles"
      contains: "text-foreground"
    - path: "src/components/CheckForUpdatesButton/CheckForUpdatesButton.tsx"
      provides: "Update button with inline Tailwind (no CSS module)"
    - path: "src/components/UpdateToast/UpdateToast.tsx"
      provides: "Toast with inline Tailwind (no CSS module)"
      contains: "sr-only"
  key_links:
    - from: "src/components/CheckForUpdatesButton/CheckForUpdatesButton.tsx"
      to: "src/lib/utils.ts"
      via: "cn() import replacing CSS module import"
      pattern: "import.*cn.*from.*@/lib/utils"
    - from: "src/components/UpdateToast/UpdateToast.tsx"
      to: "src/lib/utils.ts"
      via: "cn() import replacing CSS module import"
      pattern: "import.*cn.*from.*@/lib/utils"
---

<objective>
Convert ServerStatus, SettingsPanel (+ add DesignTokensSection), LogViewer, CheckForUpdatesButton, and UpdateToast to Tailwind utilities. Delete both CSS module files after conversion. Update existing component tests to work without CSS module imports. Add Design Tokens section to SettingsPanel as living documentation.

Purpose: Complete the monitoring/settings/utility component migration. CSS module components require import removal + file deletion. SettingsPanel gains the Design Tokens display section for ongoing token validation.
Output: 5 components converted, 2 CSS module files deleted, existing tests pass, Design Tokens section added.
</objective>

<execution_context>
@.planning/phases/11-foundation/11-RESEARCH.md
@.planning/phases/11-foundation/11-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-foundation/11-01-SUMMARY.md

<interfaces>
<!-- CSS module migration pattern (from PATTERNS.md) -->

Before (CSS module):
```tsx
import styles from "./CheckForUpdatesButton.module.css";
<div className={styles["card"]}>
```

After (Tailwind):
```tsx
import { cn } from "@/lib/utils"
<div className="bg-card border border-border rounded-lg p-4 flex flex-col gap-2">
```

<!-- Form field pattern -->
```tsx
<div className="flex flex-col gap-1.5">
  <label className="text-sm font-medium text-muted-foreground">Label</label>
  <input className={cn(
    "px-3 py-2 bg-input border border-border rounded-md text-foreground text-sm",
    "outline-none transition-colors focus:border-ring",
    hasError && "border-destructive"
  )} />
</div>
```

<!-- Section card pattern -->
```tsx
<section className="bg-card border border-border rounded-md p-5">
  <h2 className="text-lg font-semibold text-foreground mb-4">Title</h2>
</section>
```

<!-- Design Tokens section from RESEARCH.md Example 5 (lines 648-690) -->
```tsx
function DesignTokensSection() {
  const tokenGroups = [
    {
      label: "Backgrounds",
      tokens: [
        { name: "background", className: "bg-background" },
        { name: "card", className: "bg-card" },
        { name: "secondary", className: "bg-secondary" },
        { name: "muted", className: "bg-muted" },
        { name: "input", className: "bg-input" },
      ],
    },
    {
      label: "Semantic",
      tokens: [
        { name: "primary", className: "bg-primary" },
        { name: "destructive", className: "bg-destructive" },
        { name: "success", className: "bg-success" },
        { name: "warning", className: "bg-warning" },
      ],
    },
  ]

  return (
    <section className="bg-card border border-border rounded-md p-5">
      <h2 className="text-lg font-semibold mb-4 text-foreground">Design Tokens</h2>
      {tokenGroups.map(({ label, tokens }) => (
        <div key={label} className="mb-4">
          <h3 className="text-sm font-semibold text-foreground mb-2">{label}</h3>
          <div className="flex flex-wrap gap-3">
            {tokens.map(({ name, className }) => (
              <div key={name} className="flex flex-col items-center gap-1">
                <div className={cn("size-10 rounded-md border border-border", className)} />
                <span className="text-xs text-muted-foreground">{name}</span>
              </div>
            ))}
          </div>
        </div>
      ))}
    </section>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert ServerStatus, LogViewer, and SettingsPanel with DesignTokensSection</name>
  <files>src/components/monitoring/ServerStatus.tsx, src/components/LogViewer.tsx, src/components/SettingsPanel.tsx</files>
  <read_first>
    - src/components/monitoring/ServerStatus.tsx (full file — 117 lines, class references: server-status, stat-card, stat-card-label, stat-card-value, stat-card-value--large, worker-dot, worker-dot--alive, worker-dot--dead, worker-status, worker-list)
    - src/components/LogViewer.tsx (full file — 267 lines, class references: log-viewer, log-header, log-actions, log-entries, log-entry, log-level-*, log-timestamp, log-message, log-empty)
    - src/components/SettingsPanel.tsx (full file — 304 lines, class references: settings-panel, settings-form, form-field, form-field--checkbox, input-error, field-error, btn-save, section headers)
    - src/App.css (search for: server-status, stat-card, worker-dot, log-viewer, log-entry, log-level, settings-panel, settings-form classes)
    - .planning/phases/11-foundation/11-PATTERNS.md (ServerStatus section lines 491-504, SettingsPanel section lines 436-456)
    - .planning/phases/11-foundation/11-RESEARCH.md (Example 4 lines 628-644 for form fields, Example 5 lines 648-690 for DesignTokensSection)
  </read_first>
  <action>
**ServerStatus.tsx** — Add `import { cn } from "@/lib/utils"`. Convert all class references:

| Old Class | Tailwind Replacement |
|-----------|---------------------|
| `server-status` | `grid grid-cols-2 gap-4` (or whatever current grid layout is — read actual CSS) |
| `stat-card` | `bg-card border border-border rounded-md p-4` |
| `stat-card-label` | `text-sm text-muted-foreground mb-1` |
| `stat-card-value` | `text-2xl font-bold text-foreground` |
| `stat-card-value--large` | `text-3xl font-bold text-foreground` |
| `worker-dot` (alive) | `size-2.5 rounded-full bg-success` |
| `worker-dot` (dead) | `size-2.5 rounded-full bg-destructive` |
| `worker-status` | `flex items-center gap-2` |
| `worker-list` | `flex flex-wrap gap-2` |

Use cn() for conditional worker dot: `cn("size-2.5 rounded-full", worker.alive ? "bg-success" : "bg-destructive")`

**LogViewer.tsx** — Add `import { cn } from "@/lib/utils"`. Convert all class references:

Log level badge color mapping:
| Level | Tailwind Classes |
|-------|-----------------|
| error | `bg-destructive/20 text-destructive` |
| warn | `bg-warning/20 text-warning` |
| info | `bg-primary/20 text-primary` |
| debug | `bg-muted text-muted-foreground` |

Container and element mapping:
| Old Class | Tailwind Replacement |
|-----------|---------------------|
| `log-viewer` | `bg-card border border-border rounded-md overflow-hidden flex flex-col` |
| `log-header` | `flex items-center justify-between px-4 py-3 border-b border-border` |
| `log-actions` | `flex items-center gap-2` |
| `log-entries` | `flex-1 overflow-y-auto p-4 font-[family-name:var(--font-mono)] text-xs` |
| `log-entry` | `py-1 flex gap-3 border-b border-border/50` |
| `log-level-badge` | `cn("px-1.5 py-0.5 rounded text-xs font-medium uppercase min-w-[3rem] text-center", levelColors[level])` |
| `log-timestamp` | `text-muted-foreground shrink-0 whitespace-nowrap` |
| `log-message` | `text-foreground break-all` |
| `log-empty` | `text-center text-muted-foreground py-8` |

Buttons (clear, auto-scroll toggle) -> secondary button or icon button patterns.
All headings/titles need explicit typography classes.

**SettingsPanel.tsx** — Add `import { cn } from "@/lib/utils"`. Convert all class references AND add DesignTokensSection.

1. Convert existing class references:
| Old Class | Tailwind Replacement |
|-----------|---------------------|
| `settings-panel` | `space-y-6` |
| `settings-form` | section card pattern: `bg-card border border-border rounded-md p-5` |
| `form-field` | `flex flex-col gap-1.5 mb-4` |
| `form-field--checkbox` | `flex items-center gap-2 mb-4` |
| `input-error` | `border-destructive` (conditional via cn()) |
| `field-error` | `text-xs text-destructive` |
| `btn-save` | primary button pattern |
| section headings `<h2>` | `text-lg font-semibold text-foreground mb-4` |
| sub-headings `<h3>` | `text-base font-semibold text-foreground mb-3` |
| labels | `text-sm font-medium text-muted-foreground` |
| inputs | form field input pattern with cn() for error state |
| selects | `px-3 py-2 bg-input border border-border rounded-md text-foreground text-sm outline-none transition-colors focus:border-ring` |
| checkboxes | `accent-primary` |
| checkbox labels | `flex items-center gap-2 text-sm text-foreground cursor-pointer` |

2. Add DesignTokensSection at the bottom of the SettingsPanel component's return JSX, after the last settings form section but before the closing tag. Define it as a separate function component within the same file (or inline JSX — use your judgment). Include these token groups:

**Backgrounds:** background, card, secondary, muted, input
**Semantic:** primary, destructive, success, warning
**Text:** foreground (rendered as text swatch with `text-foreground bg-card`), muted-foreground (rendered as text swatch)
**Borders:** border (rendered as bordered div)

Use the DesignTokensSection pattern from interfaces section above. Add typography row showing font-family: system-ui stack and mono font.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ServerStatus.tsx contains `import { cn } from "@/lib/utils"`
    - ServerStatus.tsx contains `bg-card border border-border rounded-md`
    - ServerStatus.tsx does NOT contain `server-status` or `stat-card` as className strings
    - LogViewer.tsx contains `import { cn } from "@/lib/utils"`
    - LogViewer.tsx contains `bg-destructive/20 text-destructive` (error level)
    - LogViewer.tsx contains `font-[family-name:var(--font-mono)]` (mono font for log entries)
    - LogViewer.tsx does NOT contain `log-viewer` or `log-entry` as className strings
    - SettingsPanel.tsx contains `import { cn } from "@/lib/utils"`
    - SettingsPanel.tsx contains `bg-input border border-border` (form inputs)
    - SettingsPanel.tsx contains `border-destructive` (error state)
    - SettingsPanel.tsx contains `Design Tokens` as visible text
    - SettingsPanel.tsx contains `bg-success` (in token swatch)
    - SettingsPanel.tsx contains `bg-warning` (in token swatch)
    - SettingsPanel.tsx does NOT contain `settings-panel` or `settings-form` or `form-field` as className strings
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>ServerStatus stat cards, LogViewer log entries, and SettingsPanel form fields all use Tailwind. DesignTokensSection shows color swatches for all token groups. Zero App.css class references.</done>
</task>

<task type="auto">
  <name>Task 2: Convert CheckForUpdatesButton and UpdateToast (CSS module migration + test updates)</name>
  <files>src/components/CheckForUpdatesButton/CheckForUpdatesButton.tsx, src/components/UpdateToast/UpdateToast.tsx, src/components/CheckForUpdatesButton/CheckForUpdatesButton.module.css, src/components/UpdateToast/UpdateToast.module.css, src/components/CheckForUpdatesButton/CheckForUpdatesButton.test.tsx, src/components/UpdateToast/UpdateToast.test.tsx</files>
  <read_first>
    - src/components/CheckForUpdatesButton/CheckForUpdatesButton.tsx (full file — 91 lines)
    - src/components/CheckForUpdatesButton/CheckForUpdatesButton.module.css (full file — 78 lines)
    - src/components/CheckForUpdatesButton/CheckForUpdatesButton.test.tsx (full file — test assertions that may reference CSS module classes)
    - src/components/UpdateToast/UpdateToast.tsx (full file — 107 lines)
    - src/components/UpdateToast/UpdateToast.module.css (full file — 108 lines)
    - src/components/UpdateToast/UpdateToast.test.tsx (full file — test assertions)
    - .planning/phases/11-foundation/11-PATTERNS.md (CheckForUpdatesButton section lines 374-399, UpdateToast section lines 403-432)
    - .planning/phases/11-foundation/11-RESEARCH.md (Pattern 7 lines 416-433 for CSS module migration)
  </read_first>
  <action>
**CheckForUpdatesButton.tsx** — CSS module -> Tailwind migration:

1. REMOVE: `import styles from "./CheckForUpdatesButton.module.css";`
2. ADD: `import { cn } from "@/lib/utils"`
3. Replace ALL `styles["xxx"]` references with inline Tailwind utilities:

| CSS Module Class | Tailwind Replacement |
|-----------------|---------------------|
| `styles["card"]` | `bg-card border border-border rounded-lg p-4 flex flex-col gap-2` |
| `styles["card-header"]` | `flex items-center justify-between` |
| `styles["card-title"]` | `text-base font-semibold text-foreground` |
| `styles["check-button"]` | `px-4 py-2 rounded-md bg-success text-white font-medium text-sm min-w-24 inline-flex items-center justify-center gap-2 cursor-pointer transition-colors hover:brightness-110 disabled:opacity-60 disabled:cursor-progress` |
| `styles["spinner"]` | `size-4 border-2 border-white/40 border-t-white rounded-full animate-spin` |
| `styles["chip"]` | `text-xs px-2 py-0.5 rounded-full` |
| `styles["chip-latest"]` | `bg-success/20 text-success` |
| `styles["chip-update"]` | `bg-warning/20 text-warning` |
| `styles["chip-error"]` | `bg-destructive/20 text-destructive` |
| `styles["chip-checking"]` | `bg-primary/20 text-primary` |
| `styles["status-text"]` | `text-sm text-muted-foreground` |
| `styles["notes"]` | `text-xs text-muted-foreground mt-1 whitespace-pre-wrap` |

Read the actual module.css file first and map every class accurately. The above is a guide — actual class names may differ slightly.

4. DELETE `src/components/CheckForUpdatesButton/CheckForUpdatesButton.module.css`

**UpdateToast.tsx** — CSS module -> Tailwind migration:

1. REMOVE: `import styles from "./UpdateToast.module.css";`
2. ADD: `import { cn } from "@/lib/utils"`
3. Replace ALL `styles["xxx"]` references with inline Tailwind utilities:

| CSS Module Class | Tailwind Replacement |
|-----------------|---------------------|
| `styles["toast-root"]` | `fixed top-0 inset-x-0 z-[1000] bg-card border-b border-border shadow-lg transition-transform duration-[240ms] ease-out data-[visible=false]:-translate-y-full data-[visible=false]:pointer-events-none` |
| `styles["toast-content"]` | `max-w-2xl mx-auto px-4 py-3 flex items-center gap-4` |
| `styles["toast-icon"]` | `text-2xl shrink-0` |
| `styles["toast-headline"]` | `flex-1` |
| `styles["toast-title"]` | `text-sm font-semibold text-foreground` |
| `styles["toast-description"]` | `text-xs text-muted-foreground mt-0.5` |
| `styles["toast-actions"]` | `flex items-center gap-2 shrink-0` |
| `styles["button-primary"]` | `px-4 py-1.5 rounded-md bg-success text-white text-sm font-medium cursor-pointer transition-colors hover:brightness-110 disabled:opacity-60 disabled:cursor-progress` |
| `styles["button-secondary"]` | `px-3 py-1.5 rounded-md bg-transparent border border-border text-muted-foreground text-sm cursor-pointer transition-all hover:border-muted-foreground hover:text-foreground` |
| `styles["spinner-indeterminate"]` | `size-6 border-[3px] border-white/40 border-t-white rounded-full animate-spin` |
| `styles["progress-bar"]` | `h-1 bg-primary/20 rounded-full overflow-hidden mt-1` |
| `styles["progress-fill"]` | `h-full bg-primary rounded-full transition-[width] duration-300` |
| `styles["sr-only"]` | `sr-only` (Tailwind built-in class) |

Read the actual module.css file first and map every class accurately.

4. DELETE `src/components/UpdateToast/UpdateToast.module.css`

**Test file updates:**

For both `CheckForUpdatesButton.test.tsx` and `UpdateToast.test.tsx`:
- The tests likely don't reference CSS module class names directly (they use @testing-library queries like `screen.getByText`, `screen.getByRole`)
- BUT if any test assertions check for CSS module class names (e.g., `toHaveClass(styles.xxx)`), update them to check for Tailwind class substrings instead
- Run existing tests to verify they pass without CSS modules
- If any test imports the module.css file directly or references `styles`, remove those imports

Run tests:
```bash
npm test -- src/components/CheckForUpdatesButton/CheckForUpdatesButton.test.tsx src/components/UpdateToast/UpdateToast.test.tsx
```
  </action>
  <verify>
    <automated>npm test -- src/components/CheckForUpdatesButton src/components/UpdateToast 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CheckForUpdatesButton.tsx contains `import { cn } from "@/lib/utils"`
    - CheckForUpdatesButton.tsx does NOT contain `import styles from`
    - CheckForUpdatesButton.tsx does NOT contain `styles[`
    - CheckForUpdatesButton.tsx contains `bg-card border border-border`
    - CheckForUpdatesButton.tsx contains `bg-success text-white` (check button)
    - CheckForUpdatesButton.module.css does NOT exist (deleted)
    - UpdateToast.tsx contains `import { cn } from "@/lib/utils"`
    - UpdateToast.tsx does NOT contain `import styles from`
    - UpdateToast.tsx does NOT contain `styles[`
    - UpdateToast.tsx contains `sr-only` (Tailwind built-in)
    - UpdateToast.tsx contains `data-[visible=false]:-translate-y-full` (slide animation)
    - UpdateToast.module.css does NOT exist (deleted)
    - `npm test -- src/components/CheckForUpdatesButton src/components/UpdateToast` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Both CSS module components converted to inline Tailwind. Both module.css files deleted. Existing tests pass. sr-only uses Tailwind built-in. Toast animation uses data attribute transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries. Pure CSS class migration.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | No security-relevant changes |
</threat_model>

<verification>
1. `npm run build` exits 0
2. `npm test -- src/components/CheckForUpdatesButton src/components/UpdateToast` passes
3. CheckForUpdatesButton.module.css and UpdateToast.module.css do NOT exist
4. SettingsPanel.tsx contains "Design Tokens" text
5. grep -r "styles\[" across all 5 modified tsx files returns zero matches
6. grep -r "module.css" across all modified tsx files returns zero matches
</verification>

<success_criteria>
5 components converted: ServerStatus, SettingsPanel (with DesignTokensSection), LogViewer, CheckForUpdatesButton, UpdateToast. Both CSS module files deleted. Existing tests pass. Design Tokens section shows color swatches in settings.
</success_criteria>

<output>
After completion, create `.planning/phases/11-foundation/11-04-SUMMARY.md`
</output>
