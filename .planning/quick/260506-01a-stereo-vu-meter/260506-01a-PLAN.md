---
mode: quick-full
quick_id: 260506-01a
slug: stereo-vu-meter
description: Add stereo/mono-aware VU meter rendering
date: 2026-05-06
must_haves:
  truths:
    - VuMeter accepts channelCount prop (1 or 2, default 1)
    - Stereo renders two side-by-side bars with independent state
    - Mono renders identically to current behavior (no regression)
    - Graceful fallback when levels array shorter than channelCount
    - LevelMeter supports channelCount for stereo columns
  artifacts:
    - src/components/monitoring/VuMeter.tsx
    - src/components/monitoring/VuMeterBank.tsx
    - src/components/channels/ChannelCard.tsx
    - listener/src/components/LevelMeter.tsx
  key_links:
    - src/hooks/useAudioLevels.ts (ChannelLevelData type)
    - src/hooks/useChannels.ts (AdminChannel.outputFormat)
    - src/App.tsx (VuMeterBank caller)
---

# Quick Task 260506-01a: Stereo VU Meter

## Task 1: Refactor VuMeter for stereo support

- **files:** src/components/monitoring/VuMeter.tsx
- **action:** Add channelCount prop, extract drawBar() helper, loop over channels with independent MeterState[] per bar
- **verify:** `npm run build` passes, mono channels render single bar, stereo renders L|R with 2px gap
- **done:** channelCount=1 identical to before, channelCount=2 shows two bars

## Task 2: Wire stereo through VuMeterBank and ChannelCard

- **files:** src/components/monitoring/VuMeterBank.tsx, src/components/channels/ChannelCard.tsx
- **action:** Add outputFormat to ChannelInfo, pass channelCount to VuMeter based on outputFormat
- **verify:** `npm run build` passes, ChannelCard stereo width=32 (vs mono 24)
- **done:** VuMeterBank and ChannelCard pass correct channelCount

## Task 3: Refactor LevelMeter for stereo columns

- **files:** listener/src/components/LevelMeter.tsx
- **action:** Add channelCount prop, extract MeterColumn helper, render two side-by-side columns for stereo
- **verify:** `npx tsc --noEmit` in listener passes
- **done:** channelCount=1 identical to before, channelCount=2 shows L|R columns
