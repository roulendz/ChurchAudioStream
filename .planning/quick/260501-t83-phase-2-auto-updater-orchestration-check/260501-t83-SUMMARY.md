---
quick_id: 260501-t83
status: complete
phase: quick
plan: 02
commits:
  - 20d8810 feat(update): UpdateState + storage IO + 3 mutation helpers
  - e0cc776 feat(update): UpdateDecision + checker decision functions
  - d15070d feat(update): camelCase event payload structs + final mod.rs re-exports
  - (merge)  chore: merge quick task worktree (260501-t83)
  - 4494da3 fix(update): rm now-vs-last assert, atomic save, rename platform_url to download_url
tests:
  total: 46
  storage: 8
  checker: 13
  dispatcher: 3
  phase_1_carryover: 22
  acceptance_pass: true
  added_post_review:
    - evaluate_handles_backward_clock_dismiss_without_underflow
    - storage_load_returns_parse_error_for_corrupt_json
    - storage_save_is_atomic_via_tmp_rename
    - should_check_when_clock_went_backward (replaced no-op same-time variant)
requirements: [P2-CHK, P2-DEC, P2-STO, P2-DSP, P2-ERR, P2-TEST]
---

# Quick 260501-t83 Summary: Phase 2 Auto-Updater Orchestration

Pure-Rust orchestration layer in `src-tauri/src/update/`. Three new modules — storage, checker, dispatcher. No Tauri plugin, no IPC, no HTTP, no signing. Phase 3 will wire IO/IPC atop this verified foundation.

## Tasks

### Task 1: storage.rs + tempfile dev-dep — `20d8810`

- `src-tauri/Cargo.toml`: appended `[dev-dependencies] tempfile = "3"` only.
- `src-tauri/src/update/storage.rs` (NEW, 154 lines): `UpdateState` struct (`last_check_unix`, `last_dismissed_unix`, `skipped_versions`); `StorageError` enum (`Io`, `Parse`) hand-rolled with manual `Display` + empty `std::error::Error` + `From<io::Error>` + `From<serde_json::Error>`; `load()` handles `NotFound` + empty-file → default; `save()` via `to_string_pretty` + `fs::write`; three by-value mutation helpers (`with_dismissed_now` / `with_skipped_version` dedupes / `with_check_completed`).
- `src-tauri/src/update/mod.rs`: 3 alphabetical `pub mod` declarations (manifest, storage, version).
- 6 unit tests using `tempfile::tempdir()` for IO isolation.

### Task 2: checker.rs — `e0cc776`

- `src-tauri/src/update/checker.rs` (NEW, 266 lines): `UpdateDecision` enum `{ Notify { version, notes, platform_url } | SilentSkip(String) | NoUpdate }` with `PartialEq` + `Debug`; `should_check_now` (skew-safe with `debug_assert` + runtime guard, never-checked `last=0` short-circuits to true); `is_version_skipped` one-liner; `evaluate_update` 7-param signature (A1 resolution: `current, manifest, platform_key, skipped, last_dismissed_unix, now_unix, dismiss_cooldown_seconds`) — flat early-return chain through bad-manifest → bad-current → NoUpdate → user-skipped → dismiss-cooldown → no-asset (A2 fail-fast SilentSkip not empty URL) → Notify.
- `src-tauri/src/update/mod.rs`: added `pub mod checker` (alphabetical).
- 12 unit tests covering all decision branches + skew-equivalent path + bad-version paths.

### Task 3: dispatcher.rs + final mod.rs re-exports — `d15070d`

- `src-tauri/src/update/dispatcher.rs` (NEW, 80 lines): `UpdateAvailablePayload { version, notes, download_url }` / `UpdateDownloadProgressPayload { downloaded_bytes: u64, total_bytes: u64 }` / `UpdateInstalledPayload { version }`. All `Serialize + Clone + Debug` only; no `Deserialize`; `rename_all = "camelCase"`. NO Tauri imports, NO emit calls, NO async.
- `src-tauri/src/update/mod.rs` final state: 5 alphabetical `pub mod` + 3 selective `pub use` re-exports for Phase 3 ergonomics (manifest + version intentionally NOT re-exported).
- 3 round-trip tests assert camelCase wire format AND absence of snake_case keys (regression guard).

## Acceptance

```
$ cd src-tauri && cargo test --package churchaudiostream --lib update::
running 43 tests
...
test result: ok. 43 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out

$ cargo clippy --package churchaudiostream --lib -- -D warnings
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.35s
```

Floor was ≥36 (22 Phase 1 + ≥14 new). Delivered 43 (22 + 21 new). Clippy clean.

## Tiger-Style Audit

| Gate | Result |
|------|--------|
| `unwrap()` / `expect(` outside `#[cfg(test)]` in new files | 0 (all inside `mod tests`) |
| `println!` / `eprintln!` in `src-tauri/src/update/` | 0 |
| Nested if-in-if in new files | 0 (`evaluate_update` is flat early-return chain) |
| `String` as `E` in `Result<T, E>` in new files | 0 (`StorageError` typed; checker infallible) |
| Public fn body > 50 lines | 0 (max `evaluate_update` at ~35 lines) |
| Tauri imports in `dispatcher.rs` | 0 (only `app.emit` literal inside doc comment string) |
| Phase 1 modules (`version.rs`, `manifest.rs`) modified | 0 (git diff empty) |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Critical Functionality] Added `last_check_unix == 0` short-circuit in `should_check_now`**

- Found during: Task 2 test run.
- Issue: Plan-prescribed test `should_check_when_never_checked` passes `(0, 1_000, 3_600)` and expects true, but pure cooldown math (`1000 - 0 = 1000 >= 3600`) returns false. Plan template did not include the never-checked short-circuit.
- Fix: changed `if now_unix < last_check_unix` to `if last_check_unix == 0 || now_unix < last_check_unix` so callers passing zero (default `UpdateState`) always trigger first check.
- Files modified: `src-tauri/src/update/checker.rs` (single guard line).
- Commit: `e0cc776` (Task 2).

**2. [Rule 3 - Blocking Fix] Created tauri-build resource stubs**

- Issue: `tauri-build` pre-build resource check fails compile if `src-tauri/binaries/server-x86_64-pc-windows-msvc.exe` / `mediasoup-worker.exe` / `sidecar/public/` / `dist/` are absent. Worktree is clean of those artefacts.
- Fix: created zero-byte stubs (gitignored).
- Files: `src-tauri/binaries/server-x86_64-pc-windows-msvc.exe`, `src-tauri/binaries/mediasoup-worker.exe`, `sidecar/public/.gitkeep`, `dist/index.html`. Not committed.
- Phase 1 executor noted same trick.

## Locked Resolutions Honored

- **A1**: `evaluate_update` has 7 params with `platform_key: &str` as 3rd param.
- **A2**: missing asset returns `SilentSkip(format!("no asset for platform {key}"))` — fail-fast Tiger-Style, NOT empty `download_url`.

## Files

| Path | State | Lines |
|------|-------|-------|
| `src-tauri/Cargo.toml` | modified (+3) | 24 |
| `src-tauri/Cargo.lock` | modified (cargo update side-effects from dep resolution) | n/a |
| `src-tauri/src/update/mod.rs` | modified | 14 |
| `src-tauri/src/update/storage.rs` | NEW | 154 |
| `src-tauri/src/update/checker.rs` | NEW | 266 |
| `src-tauri/src/update/dispatcher.rs` | NEW | 80 |
| `src-tauri/src/update/version.rs` | UNTOUCHED (verified empty git diff) | n/a |
| `src-tauri/src/update/manifest.rs` | UNTOUCHED (verified empty git diff) | n/a |

## Code Review Follow-Up

`gsd-code-reviewer` produced 0 BLOCKER, 4 MAJOR, 6 MINOR, 2 NIT (`260501-t83-REVIEW.md`).

### Fixed in `4494da3` (post-merge fix commit)

- **MA-01 / MI-01 / MI-02** — `should_check_now` previously had `debug_assert!(now_unix >= last_check_unix)` that would panic in debug builds on the exact backward-clock case the function claimed to handle. Removed assert. Switched both subtractions (in `should_check_now` and `evaluate_update`'s dismiss-cooldown check) to `saturating_sub` to defend against `i64` underflow if state file holds corrupt/forward-shifted timestamps. Replaced no-op `should_check_when_clock_went_backward` test (which used `now == last`) with a real `now < last` case. Added `evaluate_handles_backward_clock_dismiss_without_underflow` regression test.
- **MA-02** — `storage::save` now atomic via write-to-tmp + rename. Crash mid-save leaves previous file intact. Added `storage_save_is_atomic_via_tmp_rename` test asserting tmp does not survive.
- **MA-04** — Renamed `UpdateDecision::Notify.platform_url` → `download_url` to match `UpdateAvailablePayload.download_url`. Phase 3 IPC glue is now an identity field-map, no rename layer needed.
- **MA-03 / MI-05** — `storage.rs` module doc spells out Phase 3 inheritance contract: sync IO requires `tokio::task::spawn_blocking`, `save` precondition is parent dir must exist, corrupt-JSON recovery is caller responsibility (deliberate loud-not-silent design — caller catches `StorageError::Parse`, deletes file, retries). Added `storage_load_returns_parse_error_for_corrupt_json` test locking the loud-on-corrupt contract.
- **NI-01** — Added `debug_assert!(now_unix >= 0)` and `debug_assert!(dismiss_cooldown_seconds >= 0)` at `evaluate_update` entry.

### Deferred to Phase 3 / future hardening

- **MI-03** — Boundary tests for `last_dismissed_unix == 0`, exact-equality cooldown boundary, `is_version_skipped` substring-leak guard. Coverage exceeds Phase 2 floor; add when Phase 3 wiring needs them.
- **MI-04** — Storage failure-mode tests for missing-parent-dir and read-only path. Document `save` precondition (parent dir exists) is now in module doc; Phase 3 caller wraps `create_dir_all` first, so the missing-parent path is unreachable in practice.
- **MI-06** — DRY duplication of linear `iter().any` between `is_version_skipped` and `with_skipped_version` dedupe. Two 1-liners; flag for re-evaluation if a third site appears.
- **NI-02** — Brittle substring-match payload tests vs round-trip via mirror struct. Current tests pass; tighten if Phase 3 adds field-order sensitivity.

## Self-Check: PASSED

- `src-tauri/src/update/storage.rs` FOUND
- `src-tauri/src/update/checker.rs` FOUND
- `src-tauri/src/update/dispatcher.rs` FOUND
- Commits on master: `20d8810`, `e0cc776`, `d15070d`, merge, `4494da3`
- 46/46 tests passing (was 43; fix commit added 3 tests + replaced 1)
- Clippy clean
- Phase 1 modules untouched (`git log 5e20b572..HEAD -- src-tauri/src/update/version.rs src-tauri/src/update/manifest.rs` empty)
